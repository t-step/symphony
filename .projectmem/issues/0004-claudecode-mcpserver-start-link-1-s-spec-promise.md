# #0004 ClaudeCode.MCPServer.start_link/1's @spec promises {:ok, start_result()} | {:error, term()}, but on a real Bandit.start_link/1 failure (reproduced via :eaddrinuse using a scratch probe script) the failure surfaces as a raw linked-process :EXIT signal, crashing any calling process that isn't trapping exits, instead of returning the documented error tuple. Reproduced directly in the new T021 test (test/symphony_elixir/claude_code_mcp_server_test.exs): a plain ExUnit test process crashed with '(EXIT ...) shutdown: failed to start child: :listener ** (EXIT) :eaddrinuse' when calling start_link/1 with a port already bound by another instance, before any assertion could run. This would also affect T022's future ClaudeCode.AppServer.start_session/2, which needs start_link/1's error path to return cleanly rather than crash the run's own process.

- 2026-08-26T03:33:26Z `issue`: ClaudeCode.MCPServer.start_link/1's @spec promises {:ok, start_result()} | {:error, term()}, but on a real Bandit.start_link/1 failure (reproduced via :eaddrinuse using a scratch probe script) the failure surfaces as a raw linked-process :EXIT signal, crashing any calling process that isn't trapping exits, instead of returning the documented error tuple. Reproduced directly in the new T021 test (test/symphony_elixir/claude_code_mcp_server_test.exs): a plain ExUnit test process crashed with '(EXIT ...) shutdown: failed to start child: :listener ** (EXIT) :eaddrinuse' when calling start_link/1 with a port already bound by another instance, before any assertion could run. This would also affect T022's future ClaudeCode.AppServer.start_session/2, which needs start_link/1's error path to return cleanly rather than crash the run's own process. [elixir/lib/symphony_elixir/claude_code/mcp_server.ex:52]
- 2026-08-26T03:33:26Z `fix`: Fixed: start_link/1 now delegates the Bandit.start_link/1 call to a new private start_bandit/3 that temporarily sets Process.flag(:trap_exit, true) around the call (save/restore via try/after), so a failed listener start (e.g. :eaddrinuse) is absorbed as the documented {:error, reason} tuple instead of crashing the caller via a raw :EXIT signal. Verified via a scratch probe script (mix run --no-start -e ...) confirming Bandit.start_link/1 returns {:error, {:shutdown, {:failed_to_start_child, :listener, :eaddrinuse}}} cleanly with trap_exit set, versus an unhandled EXIT crash without it. Covered by a new T021 regression test asserting start_link/1 returns {:error, _} (not a crash) when a second instance is started on an already-bound port. All gates clean (format, specs.check, credo --strict, dialyzer, mix test 389/0/6, mix test --cover 100% total, mix build). [elixir/lib/symphony_elixir/claude_code/mcp_server.ex:69]
