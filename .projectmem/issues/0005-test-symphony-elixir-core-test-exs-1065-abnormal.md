# #0005 test/symphony_elixir/core_test.exs:1065 'abnormal worker exit increments retry attempt progressively' is flaky under scheduler contention: asserts remaining_ms >= min_remaining_ms against a real wall-clock timer with only a ~1ms margin (observed 39499 vs required 39500 in one full-suite run alongside 140 other tests). Reproduces intermittently only under contention -- passed cleanly both in isolation and on an immediate full-suite rerun. Same class as pre-existing issue #0002 (orchestrator_status_test.exs:971), a different assertion/location. Not caused by T027/T028 (agent_execution.kind dispatch) work -- core_test.exs:1065 is untouched this session and unrelated to coding-agent dispatch resolution.

- 2026-08-26T06:46:34Z `issue`: test/symphony_elixir/core_test.exs:1065 'abnormal worker exit increments retry attempt progressively' is flaky under scheduler contention: asserts remaining_ms >= min_remaining_ms against a real wall-clock timer with only a ~1ms margin (observed 39499 vs required 39500 in one full-suite run alongside 140 other tests). Reproduces intermittently only under contention -- passed cleanly both in isolation and on an immediate full-suite rerun. Same class as pre-existing issue #0002 (orchestrator_status_test.exs:971), a different assertion/location. Not caused by T027/T028 (agent_execution.kind dispatch) work -- core_test.exs:1065 is untouched this session and unrelated to coding-agent dispatch resolution. [test/symphony_elixir/core_test.exs:1065]
