# #0007 test/symphony_elixir/core_test.exs:1025 'normal worker exit schedules active-state continuation retry' is flaky under scheduler contention (re-logged: this is issue #0006's original defect, accidentally marked fixed by an unrelated pjm fix invocation that omitted --issue -- see the correction note on #0006). Still unresolved: asserts remaining_ms >= min_remaining_ms against a real wall-clock timer with only a ~42ms margin (observed 458 vs required 500 in one full-suite run of 432 tests); passed cleanly on an immediate full-suite rerun. Same class as #0002 (orchestrator_status_test.exs:971) and #0005 (core_test.exs:1065).

- 2026-08-26T08:21:45Z `issue`: test/symphony_elixir/core_test.exs:1025 'normal worker exit schedules active-state continuation retry' is flaky under scheduler contention (re-logged: this is issue #0006's original defect, accidentally marked fixed by an unrelated pjm fix invocation that omitted --issue -- see the correction note on #0006). Still unresolved: asserts remaining_ms >= min_remaining_ms against a real wall-clock timer with only a ~42ms margin (observed 458 vs required 500 in one full-suite run of 432 tests); passed cleanly on an immediate full-suite rerun. Same class as #0002 (orchestrator_status_test.exs:971) and #0005 (core_test.exs:1065). [test/symphony_elixir/core_test.exs:1025]
- 2026-08-26T17:57:16Z `fix`: Applied review-fix pass to uncommitted T028A per independent review's one MAJOR finding: mcp_config_args/2 in ClaudeCode.AppServer ordered Symphony's own generated MCP config BEFORE the repo's .mcp.json in the composed --mcp-config invocation; since the installed Claude CLI resolves a same-name MCP server collision across multiple --mcp-config inputs by last-one-wins (empirically verified against the real native claude binary, no live API call), a repo-controlled .mcp.json declaring its own server literally named symphony_tracker could silently shadow Symphony's real tracker MCP server. Fixed by reordering to [repo, generated] so Symphony's own config is always last and therefore authoritative on any same-name collision -- symphony_tracker keeps its stable, non-randomized name (no per-run-unique renaming introduced). Added a focused regression test (claude_code_app_server_test.exs, 'repository MCP composition' describe block) asserting Symphony's generated config is always the LAST --mcp-config entry even when the repo's .mcp.json declares its own symphony_tracker server; updated the existing 'when .mcp.json exists' test's expected order to [repo, generated]. Documented the invariant in contracts/workflow-config-fields.md's 'Local execution trust model' section and appended a 'Review correction (2026-08-26)' note to research.md's R8 Correction Addendum (append-only, does not rewrite the addendum's original text). Added a code comment at repo_mcp_config_path/1 noting File.regular?/1 intentionally follows symlinks under the existing trusted-repository execution model, no new restriction added. Scope held strictly to this one fix -- did not touch T029, did not modify the paused live-e2e test artifact, did not change symphony_tracker's name, did not touch --strict-mcp-config/--setting-sources/auth model. Full gate verified clean: mix compile --warnings-as-errors, mix format --check-formatted, mix credo --strict (0 issues), mix specs.check, mix dialyzer (0 errors), mix test --cover (438 tests, 0 failures, 7 skipped, 100% coverage every module), git diff --check clean. [elixir/lib/symphony_elixir/claude_code/app_server.ex:244]
