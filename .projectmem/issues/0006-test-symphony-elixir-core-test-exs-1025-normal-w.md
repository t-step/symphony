# #0006 test/symphony_elixir/core_test.exs:1025 'normal worker exit schedules active-state continuation retry' is flaky under scheduler contention: asserts remaining_ms >= min_remaining_ms against a real wall-clock timer with only a ~42ms margin (observed 458 vs required 500 in one full-suite run of 432 tests). Reproduces intermittently only under contention -- passed cleanly on an immediate full-suite rerun (0 failures/432 tests). Same class as pre-existing issues #0002 (orchestrator_status_test.exs:971) and #0005 (core_test.exs:1065), a third distinct location. Not caused by T029/T030 (Claude Code live E2E test + Codex/hosted regression verification) work -- core_test.exs is untouched this session and this is an untouched Orchestrator retry-backoff timing code path.

- 2026-08-26T07:25:11Z `issue`: test/symphony_elixir/core_test.exs:1025 'normal worker exit schedules active-state continuation retry' is flaky under scheduler contention: asserts remaining_ms >= min_remaining_ms against a real wall-clock timer with only a ~42ms margin (observed 458 vs required 500 in one full-suite run of 432 tests). Reproduces intermittently only under contention -- passed cleanly on an immediate full-suite rerun (0 failures/432 tests). Same class as pre-existing issues #0002 (orchestrator_status_test.exs:971) and #0005 (core_test.exs:1065), a third distinct location. Not caused by T029/T030 (Claude Code live E2E test + Codex/hosted regression verification) work -- core_test.exs is untouched this session and this is an untouched Orchestrator retry-backoff timing code path. [test/symphony_elixir/core_test.exs:1025]
- 2026-08-26T08:21:01Z `fix`: Implemented tasks.md T028A (2026-08-26), the repair task inserted before T029 per research.md's R8 Correction Addendum: changed claude_code.command's schema default (config/schema.ex) from "claude --bare --permission-mode bypassPermissions --strict-mcp-config" to "claude --setting-sources project,local --permission-mode bypassPermissions --strict-mcp-config" -- the default local execution model now authenticates via the operator's existing Claude subscription/OAuth login and loads repository-scoped CLAUDE.md/hooks/settings while excluding user-global Claude Code configuration, instead of forcing API-key-only auth with zero repo context. Updated ClaudeCode.AppServer (app_server.ex): added repo_mcp_config_path/1 (computed once in start_session_resources/3, stored in the session map, File.regular?/1 so a .mcp.json that is actually a directory is treated as absent) and mcp_config_args/2 (composes the workspace's own .mcp.json, when present, alongside Symphony's generated per-run MCP config under one --mcp-config invocation in start_port/6 -- CLI confirmed to accept multiple space-separated files per invocation); stop_session/1 deliberately still only deletes Symphony's own generated file, never the repo's .mcp.json. --strict-mcp-config, --permission-mode bypassPermissions, the direct-executable/no-shell/whitespace-argv claude_code.command parsing model, and the OPENAI_API_KEY/Codex-credential environment allow-list are all unchanged. Added tests: default-command-is-scoped-not-bare (config_claude_code_worker_host_validation_test.exs), and four new cases in claude_code_app_server_test.exs's new "run_turn/4 repository MCP composition" describe block (repo .mcp.json present -> both files passed under one --mcp-config; absent -> only Symphony's file; a .mcp.json that is actually a directory -> treated as absent; an operator-supplied claude_code.command containing its own --mcp-config is not deduplicated, Symphony's own is still appended after it -- proving the pre-existing append-only contract is unchanged). Updated the two prior tests in config_claude_code_worker_host_validation_test.exs that hardcoded the old --bare default string as an arbitrary round-trip literal, to use the new default string instead (avoids stale --bare literals reading as still-endorsed). Documentation: research.md gained a new "R8 Correction Addendum (2026-08-26)" (does not rewrite R8's original text, supersedes only its default-command decision, records the primary evidence and the empirical experiment); contracts/workflow-config-fields.md gained a new "Local execution trust model and MCP composition" section documenting the allow/deny boundary, the headless-mode trust-dialog exposure (repo hooks/.mcp.json execute/connect unconditionally in claude -p, by design, relying on Symphony's existing trust boundary), the .mcp.json-is-checked-relative-to-workspace-not-source-repo assumption, and that --bare remains fully available as an explicit claude_code.command override. tasks.md gained T028A (out-of-sequence, between T028 and T029, not renumbering T029-T037) and T029's dependency now includes T028A. Deliberately did NOT touch: plan.md (its --bare mention is accurate historical narrative of what was confirmed during that planning pass, not a live claim needing correction -- matches the file's own existing precedent of layering later correction passes alongside earlier text rather than rewriting it), spec.md (no frozen FR/IV mentions --bare/auth/oauth at all -- confirmed via full-file grep before editing anything), contracts/coding-agent-behaviour.md (CodingAgent behaviour shape is unaffected -- no auth/launch-flag content lives there), and the existing uncommitted T029 live-e2e test file (belongs to the paused T029 work per explicit instruction not to casually absorb it into this repair; T029 was not started or completed this session). Full gate verified clean on this repair: mix compile --warnings-as-errors, mix format --check-formatted, mix specs.check, mix credo --strict (0 issues), mix dialyzer (0 errors), mix test --cover (437 tests, 0 failures, 7 skipped, 100% coverage on every non-exempted module), mix build (escript), git diff --check (clean). Not committed. [elixir/lib/symphony_elixir/claude_code/app_server.ex:1]
