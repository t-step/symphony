# #0001 Review of T001-T003 (structural-config capture, commit 9743745): Tracker.adapter/0 correctly pins tracker.kind's module selection (structural), but SymphonyElixir.Config.Schema.finalize_settings/1 (config/schema.ex ~line 402) still branches its Linear-specific field normalization (api_key/assignee env-indirection resolution, secret_environment_names) on the LIVE tracker.kind on every WorkflowStore reload/poll tick, not the pinned structural kind. Repro: pin kind=linear (adapter=Linear.Adapter), live-edit WORKFLOW.md to kind=memory without restart -> Tracker.bind_agent_tools().secret_environment_names silently drops from ["LINEAR_API_KEY"] to [] while Tracker.adapter() stays pinned to Linear.Adapter. This violates the 'no live behavior change before restart' guarantee IV-005/T002 is meant to deliver for tracker.kind, specifically for Linear's credential provisioning. Only Linear is affected today (the only kind-branched case in finalize_settings); other adapters (github/gitlab/jira/asana) resolve secrets independently via their own Client.secret_environment_names/1 reading the raw provider map, unaffected by this branch. Confirmed via non-destructive scratch ExUnit test outside the repo tree (job tmp dir), not committed.

- 2026-08-25T22:33:40Z `issue`: Review of T001-T003 (structural-config capture, commit 9743745): Tracker.adapter/0 correctly pins tracker.kind's module selection (structural), but SymphonyElixir.Config.Schema.finalize_settings/1 (config/schema.ex ~line 402) still branches its Linear-specific field normalization (api_key/assignee env-indirection resolution, secret_environment_names) on the LIVE tracker.kind on every WorkflowStore reload/poll tick, not the pinned structural kind. Repro: pin kind=linear (adapter=Linear.Adapter), live-edit WORKFLOW.md to kind=memory without restart -> Tracker.bind_agent_tools().secret_environment_names silently drops from ["LINEAR_API_KEY"] to [] while Tracker.adapter() stays pinned to Linear.Adapter. This violates the 'no live behavior change before restart' guarantee IV-005/T002 is meant to deliver for tracker.kind, specifically for Linear's credential provisioning. Only Linear is affected today (the only kind-branched case in finalize_settings); other adapters (github/gitlab/jira/asana) resolve secrets independently via their own Client.secret_environment_names/1 reading the raw provider map, unaffected by this branch. Confirmed via non-destructive scratch ExUnit test outside the repo tree (job tmp dir), not committed. [elixir/lib/symphony_elixir/config/schema.ex:402]
- 2026-08-25T22:43:02Z `attempt`: Threaded the WorkflowStore-pinned tracker_kind into Schema.parse/2 -> finalize_settings/2, replacing all live-kind branch reads (Linear secret/api_key resolution, active/terminal state defaults) with an effective_kind = structural_tracker_kind || live tracker.kind. WorkflowStore.reload_path/2 now calls load_state(path, state.structural.tracker_kind) so reloads normalize under the preserved pin instead of the freshly-read live kind; init/1 and the not-running fallbacks keep passing no pin (nil), which is correct since there is no prior restart to preserve at that point. tracker.kind itself (the raw field) is left reflecting the live value, unaffected -- only the kind-branch decisions are pinned. Confirmed via a scratch repro test (outside the tracked repo) that Tracker.bind_agent_tools().secret_environment_names now correctly stays ["LINEAR_API_KEY"] across a live tracker.kind edit away from linear, and correctly changes to [] only after an actual WorkflowStore restart. Extracted the kind-selection into a tiny effective_tracker_kind/2 helper to keep finalize_settings/2 under credo --strict's cyclomatic-complexity threshold (was 10, now passes). No other tracker.kind-dependent branch found elsewhere in lib/ (only config.ex:140's live nil-check, which is validation of the current file's own content and correctly unrelated to adapter-identity pinning). [elixir/lib/symphony_elixir/config/schema.ex:415] (worked)
- 2026-08-25T22:43:09Z `fix`: Verified fix for #0001: extended config_structural_snapshot_test.exs's live-edit test with bind_agent_tools().secret_environment_names assertions at each stage (pinned linear, live-edited-to-memory-before-restart, restarted-to-memory) -- all pass. Full regression: mix test 300/300 (0 failures; the pre-existing unrelated orchestrator_status_test.exs:971 wall-clock-margin flake reproduced once and was absent on 2 other runs/seeds, confirmed unrelated -- Orchestrator backoff code untouched), mix format --check-formatted clean, mix credo --strict clean, mix specs.check clean, mix dialyzer 0 errors, mix test --cover 100% total. git status clean of unintended changes. [elixir/lib/symphony_elixir/config/schema.ex:415]
