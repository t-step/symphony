# projectmem - symphony

_Last updated: 2026-08-26_

## Project purpose
Symphony turns project work (tickets in an issue tracker) into isolated, autonomous coding-agent
implementation runs: it watches a tracker (Linear/GitHub/GitLab/Jira/Asana/local), spawns an
agent (e.g. Codex) per task in an isolated workspace, and lands the resulting PR once accepted —
so engineers manage work at a higher level instead of supervising agents turn-by-turn. The repo
root only hosts the spec (`SPEC.md`) and docs; the actual reference implementation lives entirely
under `elixir/` (an Elixir/OTP + Phoenix LiveView app).

## Recent issues
- [DONE] #legacy_678e Legacy issue: Add GitLab Issues tracker adapter -> Add GitLab Issues tracker adapter (fixed)
- [DONE] #legacy_633e Legacy issue: feat(jira): honor blocking issue links (#108) -> feat(jira): honor blocking issue links (#108) (fixed)
- [DONE] #legacy_2121 Legacy issue: Fix Burrito release tag verification -> Fix Burrito release tag verification (fixed)
- [DONE] #legacy_1f32 Legacy issue: Gate Jira new-category issues on blockers -> Gate Jira new-category issues on blockers (fixed)
- [DONE] #legacy_044f Legacy issue: Add GitHub Issues tracker adapter -> Add GitHub Issues tracker adapter (fixed)
- [OPEN] #0009 test/symphony_elixir/core_test.exs:1025 'normal worker exit schedules active-state continuation retry' is flaky under scheduler contention (re-logged: this is issue #0006/#0007/#0008's original defect, accidentally marked fixed three times now by unrelated pjm fix invocations that omitted --issue -- see the correction notes on #0006, #0007, and #0008). Still unresolved: asserts remaining_ms >= min_remaining_ms against a real wall-clock timer with only a ~42ms margin (observed 458 vs required 500 in one full-suite run of 432 tests); passed cleanly on immediate full-suite reruns. Same class as #0002 (orchestrator_status_test.exs:971) and #0005 (core_test.exs:1065). Not touched by T034 (test/symphony_elixir/local_tracker_claude_code_composition_test.exs is untouched by this defect's code path). [test/symphony_elixir/core_test.exs:1025] (open)
- [DONE] #0008 test/symphony_elixir/core_test.exs:1025 'normal worker exit schedules active-state continuation retry' is flaky under scheduler contention (re-logged: this is issue #0006/#0007's original defect, accidentally marked fixed twice by unrelated pjm fix invocations that omitted --issue -- see the correction notes on #0006 and #0007). Still unresolved: asserts remaining_ms >= min_remaining_ms against a real wall-clock timer with only a ~42ms margin (observed 458 vs required 500 in one full-suite run of 432 tests); passed cleanly on an immediate full-suite rerun. Same class as #0002 (orchestrator_status_test.exs:971) and #0005 (core_test.exs:1065). [test/symphony_elixir/core_test.exs:1025] -> Implemented tasks.md T034 (001-local-tracker-multi-agent): added SC-004 equivalence test proving scheduler dispatch order, concurrency-limit enforcement, and retry/backoff decisions match between tracker.kind: local + claude_code and tracker.kind: memory + codex given equivalent normalized inputs. No production code changed. Phase 5/US3 complete. Full mix test (442 tests), mix test --cover (100%), mix credo --strict, mix dialyzer, mix specs.check, mix format --check-formatted, mix build all pass. [elixir/test/symphony_elixir/local_tracker_claude_code_composition_test.exs] (fixed)
- [DONE] #0007 test/symphony_elixir/core_test.exs:1025 'normal worker exit schedules active-state continuation retry' is flaky under scheduler contention (re-logged: this is issue #0006's original defect, accidentally marked fixed by an unrelated pjm fix invocation that omitted --issue -- see the correction note on #0006). Still unresolved: asserts remaining_ms >= min_remaining_ms against a real wall-clock timer with only a ~42ms margin (observed 458 vs required 500 in one full-suite run of 432 tests); passed cleanly on an immediate full-suite rerun. Same class as #0002 (orchestrator_status_test.exs:971) and #0005 (core_test.exs:1065). [test/symphony_elixir/core_test.exs:1025] -> Applied review-fix pass to uncommitted T028A per independent review's one MAJOR finding: mcp_config_args/2 in ClaudeCode.AppServer ordered Symphony's own generated MCP config BEFORE the repo's .mcp.json in the composed --mcp-config invocation; since the installed Claude CLI resolves a same-name MCP server collision across multiple --mcp-config inputs by last-one-wins (empirically verified against the real native claude binary, no live API call), a repo-controlled .mcp.json declaring its own server literally named symphony_tracker could silently shadow Symphony's real tracker MCP server. Fixed by reordering to [repo, generated] so Symphony's own config is always last and therefore authoritative on any same-name collision -- symphony_tracker keeps its stable, non-randomized name (no per-run-unique renaming introduced). Added a focused regression test (claude_code_app_server_test.exs, 'repository MCP composition' describe block) asserting Symphony's generated config is always the LAST --mcp-config entry even when the repo's .mcp.json declares its own symphony_tracker server; updated the existing 'when .mcp.json exists' test's expected order to [repo, generated]. Documented the invariant in contracts/workflow-config-fields.md's 'Local execution trust model' section and appended a 'Review correction (2026-08-26)' note to research.md's R8 Correction Addendum (append-only, does not rewrite the addendum's original text). Added a code comment at repo_mcp_config_path/1 noting File.regular?/1 intentionally follows symlinks under the existing trusted-repository execution model, no new restriction added. Scope held strictly to this one fix -- did not touch T029, did not modify the paused live-e2e test artifact, did not change symphony_tracker's name, did not touch --strict-mcp-config/--setting-sources/auth model. Full gate verified clean: mix compile --warnings-as-errors, mix format --check-formatted, mix credo --strict (0 issues), mix specs.check, mix dialyzer (0 errors), mix test --cover (438 tests, 0 failures, 7 skipped, 100% coverage every module), git diff --check clean. [elixir/lib/symphony_elixir/claude_code/app_server.ex:244] (fixed)
- [DONE] #0006 test/symphony_elixir/core_test.exs:1025 'normal worker exit schedules active-state continuation retry' is flaky under scheduler contention: asserts remaining_ms >= min_remaining_ms against a real wall-clock timer with only a ~42ms margin (observed 458 vs required 500 in one full-suite run of 432 tests). Reproduces intermittently only under contention -- passed cleanly on an immediate full-suite rerun (0 failures/432 tests). Same class as pre-existing issues #0002 (orchestrator_status_test.exs:971) and #0005 (core_test.exs:1065), a third distinct location. Not caused by T029/T030 (Claude Code live E2E test + Codex/hosted regression verification) work -- core_test.exs is untouched this session and this is an untouched Orchestrator retry-backoff timing code path. [test/symphony_elixir/core_test.exs:1025] -> Implemented tasks.md T028A (2026-08-26), the repair task inserted before T029 per research.md's R8 Correction Addendum: changed claude_code.command's schema default (config/schema.ex) from "claude --bare --permission-mode bypassPermissions --strict-mcp-config" to "claude --setting-sources project,local --permission-mode bypassPermissions --strict-mcp-config" -- the default local execution model now authenticates via the operator's existing Claude subscription/OAuth login and loads repository-scoped CLAUDE.md/hooks/settings while excluding user-global Claude Code configuration, instead of forcing API-key-only auth with zero repo context. Updated ClaudeCode.AppServer (app_server.ex): added repo_mcp_config_path/1 (computed once in start_session_resources/3, stored in the session map, File.regular?/1 so a .mcp.json that is actually a directory is treated as absent) and mcp_config_args/2 (composes the workspace's own .mcp.json, when present, alongside Symphony's generated per-run MCP config under one --mcp-config invocation in start_port/6 -- CLI confirmed to accept multiple space-separated files per invocation); stop_session/1 deliberately still only deletes Symphony's own generated file, never the repo's .mcp.json. --strict-mcp-config, --permission-mode bypassPermissions, the direct-executable/no-shell/whitespace-argv claude_code.command parsing model, and the OPENAI_API_KEY/Codex-credential environment allow-list are all unchanged. Added tests: default-command-is-scoped-not-bare (config_claude_code_worker_host_validation_test.exs), and four new cases in claude_code_app_server_test.exs's new "run_turn/4 repository MCP composition" describe block (repo .mcp.json present -> both files passed under one --mcp-config; absent -> only Symphony's file; a .mcp.json that is actually a directory -> treated as absent; an operator-supplied claude_code.command containing its own --mcp-config is not deduplicated, Symphony's own is still appended after it -- proving the pre-existing append-only contract is unchanged). Updated the two prior tests in config_claude_code_worker_host_validation_test.exs that hardcoded the old --bare default string as an arbitrary round-trip literal, to use the new default string instead (avoids stale --bare literals reading as still-endorsed). Documentation: research.md gained a new "R8 Correction Addendum (2026-08-26)" (does not rewrite R8's original text, supersedes only its default-command decision, records the primary evidence and the empirical experiment); contracts/workflow-config-fields.md gained a new "Local execution trust model and MCP composition" section documenting the allow/deny boundary, the headless-mode trust-dialog exposure (repo hooks/.mcp.json execute/connect unconditionally in claude -p, by design, relying on Symphony's existing trust boundary), the .mcp.json-is-checked-relative-to-workspace-not-source-repo assumption, and that --bare remains fully available as an explicit claude_code.command override. tasks.md gained T028A (out-of-sequence, between T028 and T029, not renumbering T029-T037) and T029's dependency now includes T028A. Deliberately did NOT touch: plan.md (its --bare mention is accurate historical narrative of what was confirmed during that planning pass, not a live claim needing correction -- matches the file's own existing precedent of layering later correction passes alongside earlier text rather than rewriting it), spec.md (no frozen FR/IV mentions --bare/auth/oauth at all -- confirmed via full-file grep before editing anything), contracts/coding-agent-behaviour.md (CodingAgent behaviour shape is unaffected -- no auth/launch-flag content lives there), and the existing uncommitted T029 live-e2e test file (belongs to the paused T029 work per explicit instruction not to casually absorb it into this repair; T029 was not started or completed this session). Full gate verified clean on this repair: mix compile --warnings-as-errors, mix format --check-formatted, mix specs.check, mix credo --strict (0 issues), mix dialyzer (0 errors), mix test --cover (437 tests, 0 failures, 7 skipped, 100% coverage on every non-exempted module), mix build (escript), git diff --check (clean). Not committed. [elixir/lib/symphony_elixir/claude_code/app_server.ex:1] (fixed)
- [OPEN] #0005 test/symphony_elixir/core_test.exs:1065 'abnormal worker exit increments retry attempt progressively' is flaky under scheduler contention: asserts remaining_ms >= min_remaining_ms against a real wall-clock timer with only a ~1ms margin (observed 39499 vs required 39500 in one full-suite run alongside 140 other tests). Reproduces intermittently only under contention -- passed cleanly both in isolation and on an immediate full-suite rerun. Same class as pre-existing issue #0002 (orchestrator_status_test.exs:971), a different assertion/location. Not caused by T027/T028 (agent_execution.kind dispatch) work -- core_test.exs:1065 is untouched this session and unrelated to coding-agent dispatch resolution. [test/symphony_elixir/core_test.exs:1065] (open)
- [DONE] #0004 ClaudeCode.MCPServer.start_link/1's @spec promises {:ok, start_result()} | {:error, term()}, but on a real Bandit.start_link/1 failure (reproduced via :eaddrinuse using a scratch probe script) the failure surfaces as a raw linked-process :EXIT signal, crashing any calling process that isn't trapping exits, instead of returning the documented error tuple. Reproduced directly in the new T021 test (test/symphony_elixir/claude_code_mcp_server_test.exs): a plain ExUnit test process crashed with '(EXIT ...) shutdown: failed to start child: :listener ** (EXIT) :eaddrinuse' when calling start_link/1 with a port already bound by another instance, before any assertion could run. This would also affect T022's future ClaudeCode.AppServer.start_session/2, which needs start_link/1's error path to return cleanly rather than crash the run's own process. [elixir/lib/symphony_elixir/claude_code/mcp_server.ex:52] -> Fixed: start_link/1 now delegates the Bandit.start_link/1 call to a new private start_bandit/3 that temporarily sets Process.flag(:trap_exit, true) around the call (save/restore via try/after), so a failed listener start (e.g. :eaddrinuse) is absorbed as the documented {:error, reason} tuple instead of crashing the caller via a raw :EXIT signal. Verified via a scratch probe script (mix run --no-start -e ...) confirming Bandit.start_link/1 returns {:error, {:shutdown, {:failed_to_start_child, :listener, :eaddrinuse}}} cleanly with trap_exit set, versus an unhandled EXIT crash without it. Covered by a new T021 regression test asserting start_link/1 returns {:error, _} (not a crash) when a second instance is started on an already-bound port. All gates clean (format, specs.check, credo --strict, dialyzer, mix test 389/0/6, mix test --cover 100% total, mix build). [elixir/lib/symphony_elixir/claude_code/mcp_server.ex:69] (fixed)
- [DONE] #0003 Independent review of T008-T015 (commit 913e3a2, local tracker integration layer) found a BLOCKING crash-safety gap: Local.Store.decode_store/1 (local/store.ex) validates only that the top-level JSON has format_version:1 and issues is a map -- it never validates that each issue record VALUE is itself a map. A hand-edited local_tracker.json with a malformed record (e.g. "ISSUE-1": "todo" instead of an object) passes validate_config/1 at startup (top-level shape only) but crashes Local.Adapter.to_issue/1 (adapter.ex:80, Map.get on a non-map) with BadMapError on the first fetch_issues_by_states/fetch_issues_by_ids call. This exception is unhandled all the way up through Orchestrator.handle_info(:run_poll_cycle) (orchestrator.ex, maybe_dispatch/reconcile_running_issues/reconcile_blocked_issues have no try/rescue), crashing the Orchestrator GenServer. AgentRuntimeSupervisor supervises Orchestrator and Task.Supervisor under strategy: :one_for_all (agent_runtime_supervisor.ex:32), so the crash kills Task.Supervisor too -- terminating every in-flight coding-agent Task across all issues. This directly violates FR-008.3's 'leave already-running attempts undisturbed' guarantee. Reproduced via a scratch ExUnit test (not committed) seeding a Local.Store-backed data file with a non-map issue record and calling Local.Adapter.fetch_issues_by_ids/1 directly -- confirmed BadMapError at lib/symphony_elixir/local/adapter.ex:80. [elixir/lib/symphony_elixir/local/store.ex:193] -> Fixed #0003: Local.Store.decode_store/1 (elixir/lib/symphony_elixir/local/store.ex) now validates that every value in the decoded issues map is itself a map, via a new validate_issue_records/1 helper. A non-map issue record (e.g. "ISSUE-1": "todo") is now classified as {:local_tracker_corrupt, {:invalid_shape, {:non_map_issue_record, id, record}}} -- the same corruption-error family FR-013 already uses for other on-disk corruption -- instead of reaching Local.Adapter.to_issue/1 and raising BadMapError. Both the read path (fetch_issues_by_states/ids, verified via a new local_adapter_test.exs case) and the write path (set_issue_state, verified via a new local_store_test.exs case showing the whole-store read fails safely before any partial rewrite) now return structured errors instead of raising. No try/rescue was added to Local.Adapter or Orchestrator -- the corruption boundary was fixed at its source in Local.Store per the review's explicit instruction. Added a new Orchestrator-boundary regression test (test/symphony_elixir/orchestrator_status_test.exs, 'a non-map issue record after startup...') proving this malformed-record class is skipped-and-retried like any other tracker corruption, never crashes the Orchestrator GenServer, and never disturbs an established running attempt (state.running/retry_attempts/completed all unaffected) -- confirming the AgentRuntimeSupervisor one_for_all blast radius from #0003 no longer triggers. All 6 gates verified clean: mix test 366/0/6 (4 new tests, up from 362), mix format --check-formatted, mix specs.check, mix credo --strict, mix dialyzer (0 errors), mix test --cover 100% every module, mix build (escript). Scope: fix #0003 only -- did not touch WorkflowStore.ensure_local_store/1's {:already_started,_} handling, did not begin T016, did not refactor unrelated Local.Adapter/Orchestrator/CLI/config code. [elixir/lib/symphony_elixir/local/store.ex:193] (fixed)
- [OPEN] #0002 test/symphony_elixir/orchestrator_status_test.exs 'restarts stalled workers with retry backoff' (line ~971) is flaky: asserts remaining_ms >= 9500 against a real wall-clock timer with only a ~50-150ms margin; observed failures at 9345ms and 9492ms across two runs with no code changes in between. Confirmed pre-existing/environmental, not a regression from T004-T007 (local tracker) work: file last modified 2026-07-20 (commit 3c372fa), untouched this session, and the failure reproduces in isolation on an unrelated seed. Not fixed as part of this checkpoint (out of scope of T004-T007). [test/symphony_elixir/orchestrator_status_test.exs:971] (open)
- [DONE] #0001 Review of T001-T003 (structural-config capture, commit 9743745): Tracker.adapter/0 correctly pins tracker.kind's module selection (structural), but SymphonyElixir.Config.Schema.finalize_settings/1 (config/schema.ex ~line 402) still branches its Linear-specific field normalization (api_key/assignee env-indirection resolution, secret_environment_names) on the LIVE tracker.kind on every WorkflowStore reload/poll tick, not the pinned structural kind. Repro: pin kind=linear (adapter=Linear.Adapter), live-edit WORKFLOW.md to kind=memory without restart -> Tracker.bind_agent_tools().secret_environment_names silently drops from ["LINEAR_API_KEY"] to [] while Tracker.adapter() stays pinned to Linear.Adapter. This violates the 'no live behavior change before restart' guarantee IV-005/T002 is meant to deliver for tracker.kind, specifically for Linear's credential provisioning. Only Linear is affected today (the only kind-branched case in finalize_settings); other adapters (github/gitlab/jira/asana) resolve secrets independently via their own Client.secret_environment_names/1 reading the raw provider map, unaffected by this branch. Confirmed via non-destructive scratch ExUnit test outside the repo tree (job tmp dir), not committed. [elixir/lib/symphony_elixir/config/schema.ex:402] -> Verified fix for #0001: extended config_structural_snapshot_test.exs's live-edit test with bind_agent_tools().secret_environment_names assertions at each stage (pinned linear, live-edited-to-memory-before-restart, restarted-to-memory) -- all pass. Full regression: mix test 300/300 (0 failures; the pre-existing unrelated orchestrator_status_test.exs:971 wall-clock-margin flake reproduced once and was absent on 2 other runs/seeds, confirmed unrelated -- Orchestrator backoff code untouched), mix format --check-formatted clean, mix credo --strict clean, mix specs.check clean, mix dialyzer 0 errors, mix test --cover 100% total. git status clean of unintended changes. [elixir/lib/symphony_elixir/config/schema.ex:415] (fixed)

## Decisions
- Bootstrap session (2026-08-25): initialized projectmem and Spec Kit (--integration claude) in this existing repo on branch development. DEVELOPMENT_SPEC intentionally left absent -- no spec content was written this session. Next session should begin the Spec Kit lifecycle (/speckit-constitution, /speckit-specify, etc.) to originate specification content, including for the SQLite tracker and Claude Code worker work ported from the Bindle prototype.
- Ratified Symphony Development Fork Constitution v1.0.0 at .specify/memory/constitution.md: 7 principles governing how the fork evolves relative to upstream Symphony (inherit upstream first, minimize fork delta, preserve upstream compatibility, specification before implementation, avoid unnecessary abstraction, preserve execution boundaries, verify fork behavior without regressing upstream). Deliberately excludes concrete implementation choices (tracker, storage, agent runner, packaging) -- those belong to the not-yet-written development specification. DEVELOPMENT_SPEC.md was intentionally not created this session. [.specify/memory/constitution.md]
- Ran /speckit-specify for feature 001-local-tracker-multi-agent: local durable work-tracking source (no hosted tracker/control-plane required) + multi-agent coding-agent execution boundary adding Claude Code alongside existing Codex behavior. Spec expresses new capability as FR-001..FR-012 plus IV-001..IV-006 inherited invariants (scheduler/reconciliation/retry/workspace/observability unchanged), explicit non-goals (no task decomposition, no DAG/dependency planning, no generic plugin framework, no Bindle), and 3 deliberate [NEEDS CLARIFICATION] markers deferred to a future /speckit-clarify pass: (1) can one deployment route work to >1 coding-agent execution integration concurrently, (2) does the orchestrator write local-tracker lifecycle state directly or via the existing agent-invoked tool-call pattern, (3) can the local tracker and a hosted tracker be simultaneously active in one deployment. DEVELOPMENT_SPEC.md remains absent; SPEC.md untouched; committed as 644a7ce on development and pushed to origin/development. No later Spec Kit lifecycle stage was run. [specs/001-local-tracker-multi-agent/spec.md]
- Revised specs/001-local-tracker-multi-agent/spec.md per human review: reframed feature as maintained fork behavior (not experiment/spike); resolved FR-010 (exactly one active coding-agent execution integration per deployment, no per-item runtime routing, multi-instance composition instead), FR-011 (local tracker writes flow through existing agent-invoked/host-executed tracker-write boundary, not new orchestrator APIs), FR-012 (exactly one active work-tracking source per deployment) -- removing all 3 NEEDS CLARIFICATION markers; corrected FR-002/FR-003 to keep claim/retry/concurrency/priority/scheduler-eligibility as orchestration-owned; rewrote User Story 3 as normal supported composition; strengthened SC-002 to require an actually-demonstrated successful Claude Code run; revised IV-004 for cross-integration observability without mandating identical telemetry shape; tightened FR-008 startup-vs-runtime failure handling; added FR-013 local-tracker-corruption-must-surface-visibly requirement; updated requirements checklist to reflect all clarifications resolved. No Symphony application source was touched. [specs/001-local-tracker-multi-agent/spec.md]
- Applied a second narrow human-review cleanup pass to specs/001-local-tracker-multi-agent/spec.md and its requirements checklist: FR-003 now scopes local lifecycle-state mutations to workflow/business progression via agent-invoked, host-executed tracker tooling, explicitly excluding claim/retry/reconciliation/concurrency from requiring a lifecycle mutation; User Story 1 AS2 no longer requires every successful coding-agent session to mutate lifecycle state (mutation only when workflow-directed, otherwise existing active-item continuation applies); the two remaining unanswered edge-case questions (no coding-agent integration selected -> default to Codex; no work-tracking source configured -> configuration invalid, not a hosted-tracker default) were converted to explicit resolved behavior; IV-005 now defines that an in-flight run attempt stays bound to the tracker/source and execution integration it started with, with config changes applying to future run attempts only; title changed to '...for the Symphony Fork' (drop 'Development'); User Story 3 renamed to 'Operate Symphony with Local Work Tracking and Claude Code'; Assumptions' first bullet tightened to drop the feasibility-contingency clause; User Story 2 reworded to avoid implying multiple workflows per deployment, preferring 'a deployment, configured through its WORKFLOW.md'; checklist title updated to match, no longer says 'Multi-Agent Execution'. No Symphony application source touched; no later Spec Kit lifecycle stage run; not committed. [specs/001-local-tracker-multi-agent/spec.md]
- Final pre-planning spec cleanup for 001-local-tracker-multi-agent: tightened FR-013 to distinguish permitted fresh local-tracker initialization from prohibited silent recreation of established local durable state that goes missing/corrupt/unreadable (attributed as a source-level operator-visible failure, not an item-level attempt failure); reworded 2 Edge Cases bullets to resolve the transient-vs-established-loss ambiguity and cross-reference FR-013; normalized 'local work source' -> 'local work-tracking source' terminology throughout; trimmed 2 residual defensive not-experimental phrasings. IV-005, FR-010/011/012, restart-recovery assumptions, and default-Codex/missing-tracker-invalid behavior were re-verified against current source (tracker.ex, workflow_store.ex, config.ex, codex/app_server.ex bind_agent_tools) and upstream SPEC.md and left unchanged as already correct/minimal. No implementation source touched; no later Spec Kit stage run; not committed. [specs/001-local-tracker-multi-agent/spec.md]
- Narrow correction to specs/001-local-tracker-multi-agent/spec.md: rewrote FR-008 to split failure handling into 4 explicit cases (startup-detectable config/dependency failures; coding-agent execution dependency failures at attempt launch/run -> existing attempt failure/retry path; work-tracking-source read/availability failures during polling/reconciliation -> existing tracker/source failure handling, skip poll tick/reconciliation pass and retry next one, leave running attempts undisturbed, explicitly NOT an automatic attempt failure; established local durable-state loss/corruption -> FR-013, unchanged). Fixes a mismatch where FR-008's broad 'dispatch or runtime' wording, paired with the first Edge Case's 'local tracker storage temporarily unreachable' example, implied source-availability blips become per-item attempt failures -- contradicting upstream SPEC.md Sec14.1/14.2's existing distinction (agent/attempt failures retry with backoff; tracker candidate-fetch and reconciliation state-refresh failures skip-and-retry-next-tick while keeping active workers), confirmed against orchestrator.ex's maybe_dispatch/reconcile_running_issues/reconcile_blocked_issues (all log-and-return-state-unchanged on tracker fetch error, no crash, no per-item failure, no worker termination). First Edge Case's example swapped to a genuine attempt-level dependency (coding-agent executable missing at launch) and cross-referenced to FR-008 and the second edge case; second edge case (transient vs established-state loss) reviewed and left unchanged as already correct. No other FR/IV/SC touched; FR-010/011/012/IV-005/defaults/tracker-write-boundary/scope not reopened. Not committed. [specs/001-local-tracker-multi-agent/spec.md]
- Narrow correction to specs/001-local-tracker-multi-agent/spec.md IV-005 after direct implementation verification: confirmed (not assumed) that Tracker.adapter/fetch_issues_by_states/ids (tracker.ex) resolve through live Config.settings!().tracker on every call, including every orchestrator tracker-read site (maybe_dispatch polling, reconcile_running_issues, reconcile_blocked_issues, retry revalidation in orchestrator.ex) -- no per-attempt tracker identity exists in orchestrator state. The only binding precedent, Tracker.bind_agent_tools/0 (called from codex/dynamic_tool.ex), scopes a snapshot to one coding-agent app-server session's own tool calls only, not orchestrator-wide reads. WorkflowStore.ex reloads its entire settings struct wholesale with no dynamic-vs-structural field distinction, so the prior IV-005 promised hot-switching that current Symphony cannot actually honor safely for in-flight/queued work. Upstream SPEC.md sec6.2 already permits restart-required treatment for resource-bound extension config, and Config.server_port/0's override-first pattern is an existing in-repo precedent for the same restart-oriented treatment. IV-005 rewritten: dynamic reload stays unchanged for settings Symphony already applies dynamically (polling cadence, concurrency limits, active/terminal states, workspace paths/hooks, prompt content); the selected work-tracking source and selected coding-agent execution integration are now structural deployment configuration that take effect only on a Symphony process/service restart, still configured via WORKFLOW.md. No per-attempt tracker/source routing, live migration, draining protocol, dual-active-tracker period, or generic provider registry was introduced. FR-010/FR-012/FR-008/FR-013 re-checked and left unchanged as already consistent (they never claimed hot-switching). checklists/requirements.md Notes appended (not rewritten) with this correction's rationale. No Symphony application source touched; no later Spec Kit lifecycle stage run; not committed. [specs/001-local-tracker-multi-agent/spec.md:97]
- Ran /speckit-plan for feature 001-local-tracker-multi-agent against the frozen spec (no requirements reopened). Generated plan.md, research.md, data-model.md, contracts/{coding-agent-behaviour,local-tracker-adapter,workflow-config-fields}.md, and quickstart.md under specs/001-local-tracker-multi-agent/. Key design decisions: (1) local work-tracking source = single durable JSON file (default .symphony/local_tracker.json, atomic write-temp+rename), no new dependency (Ecto stays config-only, no SQL/ORM added); file-absence = not-yet-established (safe to init fresh), any other read failure = FR-013 operator-visible corruption, never silently reset. (2) Local tracker lifecycle writes reuse the existing OPTIONAL Tracker agent_tool_specs/execute_agent_tool boundary (one new tool, local_tracker_set_state, scoped to the session's bound issue only) -- no new orchestrator write API, per FR-011. (3) New SymphonyElixir.CodingAgent behaviour extracted verbatim from Codex.AppServer's existing public shape (start_session/2, run_turn/4, stop_session/1); Codex.AppServer retrofitted with the @behaviour annotation only, zero functional change; AgentRunner resolves the module via a new agent_execution.kind WORKFLOW.md field (default codex, restart-only per IV-005) instead of hardcoding Codex.AppServer. (4) Claude Code integration (lib/symphony_elixir/claude_code/) launches the claude CLI per-turn via -p/--output-format stream-json (fresh process per turn, --resume <session_id> for continuation -- structurally different from Codex's one-long-lived-process model; this asymmetry is explicitly allowed by the CodingAgent contract, not worked around), and exposes tracker agent tools via a host-generated per-run MCP config (--mcp-config) + a new stdio MCP server module instead of Codex's proprietary dynamicTools channel. Several exact Claude Code CLI flag/enum names (permission-mode value, stream-json event names) are flagged low-confidence in research.md R5/R7/R8 pending re-verification against the installed CLI version during implementation -- not treated as settled fact. (5) No rename of the ~74 existing codex_*-prefixed telemetry field names in orchestrator.ex/status_dashboard.ex/presenter.ex; both integrations populate the same fields (IV-004 allows non-identical shape; Principle II weighs against the rename sweep). Constitution Check (v1.0.0) passed pre- and post-design with no violations; Complexity Tracking table empty. No Symphony application source touched; tasks.md not yet generated (next step is /speckit-tasks). [specs/001-local-tracker-multi-agent/plan.md]
- Corrected 7 design gaps in specs/001-local-tracker-multi-agent/ planning artifacts (plan.md, research.md, data-model.md, contracts/*.md, quickstart.md) per human review, without reopening the frozen spec.md or running any later Spec Kit stage. (1) FR-013 restart-establishment: file-absence-alone cannot distinguish never-initialized from established-then-deleted; added a second, independently-durable sibling marker file (<path>.established) plus a documented open-time decision table (fresh/self-heal/established-loss/corrupt), since a marker embedded in the same data file cannot survive that file's own deletion. (2) Claude Code MCP process boundary: verified Codex.DynamicTool.execute/4 is called in-process from Codex.AppServer's own stdio receive loop (same BEAM, same Port) -- a stdio MCP server spawned as claude's own child OS process has no such path back into Symphony's BEAM. Corrected to: Symphony hosts the MCP server itself as a Bandit HTTP endpoint (Bandit already a dependency, same library SPEC.md section13.7's optional observability extension uses) bound to 127.0.0.1:0; claude connects out to it via a --mcp-config type:http entry (confirmed supported via claude mcp add --transport http and code.claude.com/docs/en/mcp). No second OS process, no new dependency, no distributed Erlang. (3) CodingAgent session contract: confirmed by reading agent_runner.ex:117-126 that AgentRunner already always reuses the original start_session/2 result for every turn (never threads run_turn's return forward), and Codex.AppServer.run_turn/4's actual return map has no session key -- the contract's prior 'run_turn MAY return an updated session, AgentRunner MUST thread it' was unimplemented and, once Claude Code pre-assigns its own session id via the confirmed --session-id <uuid> CLI flag at start_session/2 (mirroring how Codex's thread_id is fixed once at start_session per upstream SPEC.md section10.2), also unnecessary. Contract corrected to: session is fixed at start_session/2 for both integrations; run_turn/4 returns only {:ok, turn_result}. (4) tracker.provider.path: confirmed via workflow_store.ex/tracker.ex trace that every tracker read pulls Config.settings!().tracker fresh with no per-attempt snapshot, so a live path change for tracker.kind:local would swap the orchestrator to a different data source's identity mid-flight (issues silently 'disappearing' per the omission-means-deleted adapter rule) -- made tracker.provider.path structural/restart-only specifically for tracker.kind:local, leaving every other tracker kind's provider.* fields dynamically reloadable as before. (5) dispatchable: confirmed via all 5 existing adapters that dispatchable is adapter-specific structural filtering (PR/section/completed-task exclusion), not a uniform archived/withdrawn flag, and no archived concept exists anywhere in the codebase; GitLab already hardcodes dispatchable:true unconditionally as precedent -- local tracker now does the same, removing the previously-undefined archived field entirely. (6) Concurrency: confirmed orchestrator.ex spawns one Task.Supervisor child per dispatched issue (genuinely concurrent), and no existing adapter writes a shared local file from multiple callers -- added a Local.Store GenServer singleton that serializes all reads/the one write, reaffirming the single-JSON-file choice (R1) rather than switching to SQLite, since SQLite would need the same serialization discipline anyway. (7) Firmed up Claude Code CLI research via claude --help (installed v2.1.245) and code.claude.com/docs: confirmed --verbose required for stream-json streaming, --permission-mode bypassPermissions is a real enum value, --bare is the documented mechanism for forced ANTHROPIC_API_KEY-only auth (no OAuth/keychain fallback), and --mcp-config supports type:http remote servers -- added --bare and --strict-mcp-config to the launch contract. Only remaining low-confidence item: exact stream-json event-type names, deferred to implementation-time verification. Constitution Check re-run and passed with no violations. Files changed: plan.md, research.md, data-model.md, contracts/coding-agent-behaviour.md, contracts/local-tracker-adapter.md, contracts/workflow-config-fields.md, quickstart.md. spec.md untouched; no later Spec Kit stage (tasks/clarify/analyze/implement) run; no Symphony application source touched; not committed. [specs/001-local-tracker-multi-agent/research.md]
- Second, tightly-scoped planning-review pass on specs/001-local-tracker-multi-agent/ (plan.md, research.md, data-model.md, contracts/*.md, quickstart.md), spec.md still frozen/untouched, no later Spec Kit stage run. Resolved 2 remaining design gaps a human review found in the prior correction pass. (1) FR-013 restart-establishment: replaced the marker-with-self-heal design (Local.Store silently writing an establishment marker whenever ordinary polling observed data-present/marker-absent) with fully explicit initialization -- Symphony's running process (startup validation, polling, dispatch, Local.Store's GenServer read path) now NEVER writes the data file's initial content or the marker; only a new, separate operation, 'symphony local-tracker init' (a packaged-CLI subcommand on CLI.evaluate/2, confirmed necessary over a mix task since the production Burrito single-binary target has no mix/Elixir toolchain -- workflow_store.ex/cli.ex/mix.exs traced directly), performs the one-time atomic two-file creation, is idempotent-safe to re-run over already-valid data (completes establishment by writing only the marker), and requires an explicit --reset flag to discard existing data. This mirrors how every hosted tracker's underlying project/board must already be provisioned before Symphony points at it -- confirmed Local.Adapter.validate_config/1 slots into the exact same WorkflowStore.init/1 -> Config.validate_settings/1 -> Tracker.validate_config/1 startup-failure chain every hosted adapter's credential check already uses, zero new plumbing. Two new error categories: local_tracker_not_initialized (both files absent) and local_tracker_ambiguous_state (data present, marker absent -- partial restore/interrupted init/hand-placed file -- never auto-resolved). Also corrected the prior pass's .gitignore claim: .codex/ is not blanket-ignored in this repo, only .codex/original-user-prompt.txt specifically -- .symphony/'s two default file paths are now gitignored by literal path to match the real precedent, not a blanket directory ignore. (2) Claude Code MCP topology/session-binding/auth/remote-worker: fully specified process topology (one ClaudeCode.MCPServer Bandit listener per coding-agent RUN, not per process/host/turn -- started in start_session/2, stopped in stop_session/1, reused across a run's turns, dies with its process tree on abnormal exit via ordinary OTP linking) and session/issue binding (Tracker.bind_agent_tools/0's binding + current issue captured once and held in that one process's own state -- no lookup table, no global current-issue state, structurally impossible for one run's listener to reach another run's binding) and authentication (per-run listener plus a per-run unguessable bearer token in the MCP URL path, defense in depth against another local process discovering the ephemeral port). Most significantly: investigated Symphony's actual worker_host/SSH remote-execution model directly (ssh.ex, workspace.ex, codex/app_server.ex) and confirmed it has NO shared filesystem between orchestrator and worker hosts and NO existing remote-file-delivery mechanism (no scp/rsync anywhere) -- a naive Bandit listener on 127.0.0.1 would be unreachable from a remote worker's own loopback, and SSH reverse-port-forwarding (-R) has no existing precedent and depends on remote sshd AllowTcpForwarding Symphony cannot verify or control. Confirmed worker_host/SSH is not mentioned anywhere in frozen spec.md or upstream SPEC.md as a requirement, and spec.md's own Assumptions explicitly permit Claude Code not exposing every Codex-specific capability. Decision: Claude Code execution is explicitly local-execution-only in this feature; agent_execution.kind: claude_code combined with a non-empty worker.ssh_hosts is now invalid configuration, enforced as a loud startup validation failure (same failure class as any other bad config) plus defense-in-depth in ClaudeCode.AppServer.start_session/2 itself. Existing Codex worker_host behavior is completely unmodified -- this is a documented, spec-compliant scope boundary, not a silently dropped capability, and avoids inventing a materially larger, unverifiable remote-delivery/forwarding subsystem for a capability nothing requires. Constitution re-checked and still passes with no violations (documented honestly in plan.md's new 'Second Correction Pass Re-Check' section, including an explicit accounting of what this pass adds -- one CLI subcommand, one cross-field validation rule, two error categories -- versus what it removes -- the self-heal inference protocol). quickstart.md rewritten to validate the full final design end to end: fresh init requirement, idempotent re-init, normal restart, established-loss while stopped (resolved via re-init) and while running, deliberate --reset, concurrent lifecycle-mutation safety, MCP tool calls landing on the correct bound issue, two concurrent Claude-backed runs unable to cross-call each other's tracker context, remote-worker rejection, and the unmodified Codex+hosted-tracker regression path. All artifacts independently re-read front-to-back for consistency before committing; no Symphony application source touched; not yet committed at time of this log entry. [specs/001-local-tracker-multi-agent/research.md]
- Ran /speckit-tasks for 001-local-tracker-multi-agent against the frozen spec/plan/research/data-model/contracts/quickstart (all committed at 17e7c57 on development). Generated tasks.md: 37 tasks (T001-T037) across 6 phases -- Setup(1), Foundational(2: a new structural-config-capture mechanism for tracker.kind/tracker.provider.path-when-local/agent_execution.kind, required because WorkflowStore/Tracker.adapter currently re-read Config.settings!() fresh on every call with no per-field structural/dynamic split, which would otherwise let a live WORKFLOW.md edit to either selector take effect before restart, violating IV-005/research.md R9a), US1 local tracker (12 tasks: local/store.ex, local/init.ex as deliberately separate modules per plan's structure decision, local/adapter.ex, local/agent_tool.ex, tracker.ex registration, cli.ex local-tracker init subcommand, plus tests for R2's full decision table, concurrent-writer serialization, and orchestrator skip-tick failure handling), US2 Claude Code (15 tasks: an explicit CLI-verification task (T016) gating implementation of claude_code/app_server.ex, coding_agent.ex behaviour extraction + Codex retrofit-only regression, claude_code/mcp_server.ex Bandit HTTP MCP listener, config schema/validation for agent_execution.kind + claude_code embed + worker_host cross-field rejection, agent_runner.ex dispatch resolution, live e2e test, full existing regression suite run), US3 composition (4 verification-only tasks: end-to-end composition, MCP-tool-reaches-correct-issue, concurrent-runs-cannot-cross-call, SC-004 equivalence), Polish (3: AGENTS.md doc update, make all gate, full quickstart re-run). Did not modify spec.md or any existing planning artifact; no later Spec Kit stage (analyze/implement/converge) run. No Symphony application source touched. Not committed. [specs/001-local-tracker-multi-agent/tasks.md] [specs/001-local-tracker-multi-agent/tasks.md]
- Implemented tasks.md T001-T003 (Setup + Foundational phases) for 001-local-tracker-multi-agent, scoped narrowly per explicit instruction to stop before any User Story work. T001: added .symphony/local_tracker.json(.established) to elixir/.gitignore by literal path. T002/T003 (structural-configuration capture, IV-005/research.md R9): added a :structural field to WorkflowStore.State (%{tracker_kind: ...}), computed once in init/1 and preserved verbatim across every later reload_path/2 regardless of how many times WORKFLOW.md changes afterward; exposed via WorkflowStore.structural_settings/0 and Config.structural_settings!/0 (mirrors Config.settings!/0's existing not-running fallback pattern); Tracker.adapter/0 (and therefore bind_agent_tools/0) now resolves the active adapter module from this pinned snapshot instead of a live Config.settings!().tracker.kind read -- the smallest extension of WorkflowStore's existing settings-ownership, no second config store/provider registry introduced. Deliberately deferred (not built now, to avoid untested/dead code): tracker.provider.path pinning-when-local, since 'local' is not yet a registered Tracker adapter (deferred to T008); agent_execution.kind pinning, since that schema field does not exist yet (deferred to T025, per T025's own already-existing text) -- tasks.md T002/T003/T008 text updated to record this split explicitly. Added test/symphony_elixir/config_structural_snapshot_test.exs (4 tests, 100% coverage on workflow_store.ex). Discovered and fixed a significant pre-existing-test-suite consequence of this IV-005 behavior change: ~40 test files share one long-lived WorkflowStore singleton across the whole mix test run (never restarted between tests), and dozens of tests relied on write_workflow_file!/2's force_reload() to make a just-written tracker_kind take effect immediately -- now structurally impossible without an actual restart. Fixed per AGENTS.md's 'fix the shared harness, not each test' guidance: added restart_workflow_store!/0 to test/support/test_support.exs (Supervisor.terminate_child+restart_child, monitored) and wired it into TestSupport's own per-test setup block (so every test starts with a correctly-pinned structural snapshot matching its own default WORKFLOW.md) plus 7 individual tests that switch tracker_kind mid-test (dynamic_tool_test.exs, asana/github/gitlab/jira_adapter_test.exs, extensions_test.exs, core_test.exs x3 sites). Deliberately did NOT change write_workflow_file!/2 itself to always restart, since that would break the existing, still-required 'invalid reload keeps last known good config' resilience tests (core_test.exs 'runtime restart keeps last good settings after an invalid reload', extensions_test.exs's malformed-YAML/semantic-invalid-config sequence) -- restart-vs-reload choice is now explicit per call site, not implicit. Verified via 7+ repeated full-suite runs with varying seeds: two pre-existing, unrelated flaky timing tests surfaced sporadically (core_test.exs 'abnormal worker exit increments retry attempt progressively', orchestrator_status_test.exs:971 stall-detection backoff) -- both are tight wall-clock margin assertions (Process.sleep+monotonic-time windows of 500-1000ms) on code paths I never touched (Orchestrator backoff math unchanged), confirmed pre-existing and unrelated to this change. make all (fmt-check, mix specs.check, credo --strict, coverage 100%, dialyzer 0 errors) passes clean. Not yet committed at time of this log entry; config/schema.ex was touched then fully reverted (local-tracker default-path/active-states additions belong to T008, not T002) after realizing it introduced US1 scope. [elixir/lib/symphony_elixir/workflow_store.ex] [elixir/lib/symphony_elixir/workflow_store.ex]
- Populated PROJECT_MAP.md (Project purpose, Structure, Relationships) which was still placeholder despite summary.md having substantial real history; now starting T004-T007 (Local.Store persistence + explicit init) for specs/001-local-tracker-multi-agent per user's scoped session request. [specs/001-local-tracker-multi-agent/tasks.md]
- Completed T004-T007 for specs/001-local-tracker-multi-agent: implemented SymphonyElixir.Local.Store (lib/symphony_elixir/local/store.ex, GenServer serializing R2's read decision table + the one lifecycle write, atomic write-temp+rename via Store.atomic_write/2) and SymphonyElixir.Local.Init (lib/symphony_elixir/local/init.ex, the only code path that writes establishment state, sharing Store.marker_state/1 + Store.read_data_file/1 for identical decision-table logic without a running GenServer). Store does not force process registration (start_link accepts :name via opts) so tests can run isolated stores concurrently; production wiring under tracker.kind: local's structural pin is deferred to T008 as instructed. Local.Init takes an already-resolved data path, not a WORKFLOW.md path -- CLI loading/resolution is T012's job. All 4 repo gates (mix test, format --check-formatted, mix lint [specs.check + credo --strict], mix dialyzer, mix test --cover at the 100% threshold) pass; one pre-existing flaky test (issue #0002) is unrelated and untouched. [elixir/lib/symphony_elixir/local/]
- Independent review of commit 5440719 (T004-T007, Local.Store + Local.Init): verified against frozen spec FR-013, research.md R2/R2a/R1a decision tables, and data-model.md by direct code inspection (store.ex, init.ex) plus 33 focused + 333 full-suite tests, mix format, mix lint (specs.check+credo --strict), mix dialyzer (0 errors), mix test --cover (100% total, Local.Store/Local.Init both 100%) -- all pass; the known pre-existing orchestrator_status_test.exs:971 wall-clock flake (#0002) reproduced once, confirmed unrelated. Verdict: SAFE to build T008-T015 on. FR-013's full read-decision table (not-initialized/ambiguous/established/corrupt, including a corrupt-or-unreadable marker) is faithfully implemented in Store.do_read/1 with zero paths where ordinary runtime (read or the one set_issue_state lifecycle write) creates or completes either file; Init is verified as the sole establishment path with correct data-then-marker crash-safe ordering, safe idempotent marker-completion over valid data, and refusal on unparseable data or already-established stores without --reset. Concurrency is correctly closed: Store's single GenServer mailbox serializes read-modify-write, proven by a two-concurrent-issue test that would fail under a stale-snapshot lost-update race. atomic_write/2 places its temp file in the same directory as the target (valid same-filesystem rename) with a System.unique_integer-suffixed name (collision-free within one BEAM VM; cross-OS-process collision is the same already-disclaimed no-cross-process-locking scope as every other local-store guarantee). No premature wiring found: grep confirms zero references to Local.* outside local/store.ex and local/init.ex themselves, and tasks.md's diff in this commit only flips T004-T007 to [x]. Two Minor, non-blocking findings recorded for awareness before T008: (1) Store's atomic_write/2 calls File.mkdir_p(dir) unconditionally, so if a store's whole directory were deleted out-of-band in the narrow window between do_read and atomic_write inside one set_issue_state call, the write would silently recreate the directory and rewrite a full data file from the in-memory snapshot with no marker -- this degrades to R2's already-defined 'ambiguous, never auto-resolved' bucket on the next read (fail-safe, not a silent fresh-store reset), not a new hazard, but worth knowing about. (2) Local.Init.do_reset/2 deletes the data file before the marker; if remove_if_exists(data_path) succeeds but remove_if_exists(marker_path) fails partway (e.g. a permission or external-interference edge case), the store is left in the 'marker present, data absent' shape -- which is FR-013's established-state-loss category. This is an accurate, fail-safe, non-silent categorization of what actually happened (real data was actually deleted), not a false-healthy state, but could read to an operator as 'established data was lost' rather than 'a --reset partially failed'; the immediate Init.run/2 caller does still receive the raw {:error, reason} from the failed remove_if_exists call, so this is not silently swallowed. Neither finding blocks T008. [elixir/lib/symphony_elixir/local/store.ex]
- Implemented T008-T015 (local tracker integration layer) for 001-local-tracker-multi-agent. Local.Store singleton lifecycle is tied to WorkflowStore's own restart boundary: WorkflowStore.init/1 (after computing structural.tracker_provider_path via Local.Adapter.resolve_provider_path/2) calls a new ensure_local_store/1 that start_link's Local.Store under the fixed name SymphonyElixir.Local.Store, linked to WorkflowStore's own process (default GenServer.start_link linking) -- so a WorkflowStore restart (the only event that can change the pinned path, IV-005/R9a) always tears down and restarts the linked Local.Store too, while an ordinary dynamic reload (force_reload/poll tick) never touches it. No new supervisor/DynamicSupervisor was introduced; Local.Store.start_link/1 itself (T004) was left untouched. {:error, {:already_started, _}} is treated as :ok (idempotent) to tolerate a narrow theoretical restart race; covered by a dedicated test. [elixir/lib/symphony_elixir/workflow_store.ex:238]
- Local.Adapter.validate_config/1 resolves an ordering hazard: WorkflowStore.init/1's own Config.validate_settings/1 call (inside load_state/2) runs BEFORE compute_structural/ensure_local_store, so validate_config cannot depend on the Local.Store singleton being started yet. Fixed by extracting Local.Store's do_read/1 decision-table logic into a new public, process-free Store.evaluate/1(data_path) -- a pure refactor of T004 with zero behavior change (do_read/1 now just delegates to it) -- and having validate_config/1 call Store.evaluate(resolved_path) directly instead of going through the GenServer. Ordinary dispatch reads (fetch_issues_by_states/ids) still go through the GenServer-backed Store.read() as intended. [elixir/lib/symphony_elixir/local/adapter.ex:32]
- tracker.provider.path resolution for tracker.kind: local mirrors workspace.root's existing two-step pattern: Config.Schema.finalize_settings/2 token-resolves ($VAR expansion, default fallback) provider["path"] via the existing resolve_path_value/2 helper at parse time (stays relative); the workflow-dir join (~ expansion + relative-path join, via Path.expand/2) happens later in Local.Adapter.resolve_provider_path/2, called from WorkflowStore.compute_structural/2 (structural, pinned) and from Local.Adapter.validate_config/1 (live, unpinned -- validate_config always resolves against the current live WORKFLOW.md path, matching every other adapter's validate_config; only the singleton's own stored path is restart-pinned). Config.workflow_dir/0 was made public (was private) to share this join logic with Local.Adapter without duplicating it. [elixir/lib/symphony_elixir/config/schema.ex:445]
- CLI.evaluate/2's return contract extended additively with a new {:done, message} variant (alongside the existing :ok and {:error, message}) to support symphony local-tracker init [-reset] [WORKFLOW.md] (T012) as a one-shot operation that must print-and-exit(0) without starting the orchestrator/wait_for_shutdown -- distinct from :ok, which still means "orchestrator started, wait for shutdown" for every pre-existing invocation shape, left completely unchanged. deps gained load_workflow/local_tracker_init keys (defaulting to Workflow.load/1 and Local.Init.run/2) purely for this new branch; every existing deps consumer/test is unaffected. [elixir/lib/symphony_elixir/cli.ex:45]
- Independent review verdict for T008-T015 checkpoint (commit 913e3a2): ready after minor fixes, not ready for T016 yet. Verified independently (not from the implementation report): all 6 repo gates pass (mix test 362/0/6 across 5 seeded runs incl. the 0.0.2 default -- pre-existing flaky orchestrator_status_test.exs:971 (#0002) did not reproduce in any run; mix format --check-formatted clean; mix specs.check clean; mix credo --strict 0 issues; mix dialyzer 0 errors; mix test --cover 100% every module incl. Local.Adapter/AgentTool/Init/Store). Config error taxonomy (not_initialized/ambiguous/corrupt/invalid_path), path resolution (tracker.provider.path pinned restart-only under tracker.kind:local, verified end to end incl. live-edit-then-restart), and the agent-tool authorization boundary (local_tracker_set_state scoped strictly to opts[:issue], cannot spoof id, input schema has additionalProperties:false) are all correctly implemented and well-tested. The one BLOCKING finding is logged separately as issue #0003 (malformed local-tracker JSON record crashes Local.Adapter.to_issue/1 -> kills Orchestrator -> one_for_all supervisor kills all running agent Tasks). Race-window investigation: independently stress-tested the WorkflowStore.ensure_local_store/1 {:error, {:already_started, pid}} tolerance for a genuinely-stale (about-to-die) Local.Store singleton, using two real restart mechanisms (Supervisor.terminate_child+restart_child matching restart_workflow_store!/0's test helper, and abrupt Process.exit(:kill)+supervisor auto-restart matching a real production crash) under heavy scheduler contention (busy_loop noise on every core). 0 stale-singleton hits across ~38,000 combined iterations once a false-positive in the stress harness itself (checking Local.Store's registration before the new WorkflowStore's init/1 had actually finished, caught 8/5000 times) was corrected by synchronizing on the new process's init completion via a :ping call. Conclusion: the race is mechanistically conceivable per OTP's asynchronous EXIT-signal-to-linked-process semantics, but not reproducible in practice -- classified as theoretical/harmless, no fix or new supervisor/registry machinery recommended. Plan divergences (Store.evaluate/1 extraction, Config.workflow_dir/0 going public, CLI.evaluate/2 gaining {:done,_}, resolve_tracker_provider/2+resolve_tracker_states/2 extraction) all reviewed and judged justified/minimal, not scope creep.
- Implemented T016 (001-local-tracker-multi-agent) as its actual tasks.md scope: a CLI-verification/documentation task, not an implementation task -- despite the session's initiating prompt assuming T016 was a Claude-Code-tracker-integration implementation task with tests/authorization boundaries (that description matches T020-T028, not T016). Verified all 6 of R5/R7/R8's confidence-flagged claude CLI flags (--session-id, --resume, --output-format stream-json/--verbose/--include-partial-messages, --permission-mode bypassPermissions, --bare, --strict-mcp-config) directly against 'claude --help' on the installed v2.1.246 -- all match documented semantics exactly, no corrections to R5/R8 needed. Live claude -p turn (user-run, this session's own auto-mode classifier blocks nested claude CLI invocation from Bash) captured the actual stream-json event-type inventory R5's confidence note asked for: top-level types system(hook_started/hook_response/init/status)/stream_event(message_start/content_block_start/content_block_delta/content_block_stop/message_delta/message_stop)/assistant/rate_limit_event/result -- the terminal result event (subtype/is_error/result/session_id/usage/stop_reason) is the turn-completion signal T022's parser should key off, and system/init is the session-start signal. Confirmed R7's core mechanism end-to-end: the caller-supplied --session-id UUID is echoed unchanged on every event. Left explicitly open (documented in research.md, not guessed): --resume turn-to-turn continuation (only one turn was run), tool-call event shapes (no tool use in this prompt), and the failure-path result shape (is_error:true case) -- all deferred to T020-T022's own implementation/tests where they'll be exercised for real. Findings recorded as a dated addendum in research.md only, per T016's explicit instruction; no other planning artifact touched, no application source touched, T017+ not started. [specs/001-local-tracker-multi-agent/research.md]
- Implemented T017 (001-local-tracker-multi-agent): defined SymphonyElixir.CodingAgent behaviour in lib/symphony_elixir/coding_agent.ex per contracts/coding-agent-behaviour.md -- 3 callbacks (start_session/2, run_turn/4, stop_session/1), no @optional_callbacks (all required per contract, unlike Tracker's optional agent_tool_specs/execute_agent_tool/validate_config), issue param typed as SymphonyElixir.Tracker.Issue.t(). Scope held to the literal task: no @behaviour annotation added to Codex.AppServer (that is T018), no test file added (T017 is a pure compile-time typespec declaration with no runtime behavior to assert -- T019 adds coding_agent_test.exs once T018's retrofit gives it something to check). All 6 gates verified: mix format clean, mix specs.check clean (no public functions, only @callback), mix credo --strict 0 issues, mix test 366/0/6 (1 known pre-existing flaky wall-clock test at core_test.exs:1065 failed once then passed clean on rerun, confirmed unrelated -- untouched code path), mix dialyzer 0 errors, mix test --cover 100% every module incl. new SymphonyElixir.CodingAgent, mix build (escript) succeeds. git diff --check clean. T018-T037 untouched. [elixir/lib/symphony_elixir/coding_agent.ex:1]
- Implemented T018 (001-local-tracker-multi-agent) as its literal annotation-only scope: added @behaviour SymphonyElixir.CodingAgent to lib/symphony_elixir/codex/app_server.ex, no other change. Verified before editing (not assumed): Codex.AppServer.start_session/2 returns {:ok, session}|{:error, term()}; run_turn/4 returns {:ok, %{result, session_id, thread_id, turn_id}}|{:error, reason} (no session-shaped return, matching the contract's corrected no-threading semantics); stop_session/1 (via stop_port/1) always returns :ok including when the port is already gone. mix compile --warnings-as-errors and mix dialyzer (0 errors) both confirmed zero callback/type mismatch -- the contract's issue :: Issue.t() vs run_turn/4's existing @spec issue :: map() is compatible (Issue.t() is a subtype of map(), contravariant argument widening in the impl is fine) and produced no warning. All 7 gates verified clean from e434a40's baseline: mix format --check-formatted, mix specs.check, mix credo --strict (0 issues), mix test 366/0/6 clean run (the known pre-existing wall-clock flake at core_test.exs:1065 reproduced twice more under --cover instrumentation overhead across repeated runs -- confirmed unrelated via isolated rerun and code-path review, Orchestrator retry-backoff timing math untouched by this change), mix dialyzer 0 errors, mix test --cover 100% total (Codex.AppServer itself is in mix.exs's coverage ignore_modules list, unaffected either way), mix build (escript) succeeds, git diff --check clean. tasks.md T018 checked off only; T019-T037 untouched; no test file added (T019's explicit job per its own task text). Did not rename functions, change start_session/2/run_turn/4/stop_session/1 signatures, touch AgentRunner, add provider selection, or begin any T019+ work. [elixir/lib/symphony_elixir/codex/app_server.ex:6]
- Implemented T019 (001-local-tracker-multi-agent) as its literal scope: added test/symphony_elixir/coding_agent_test.exs asserting Codex.AppServer satisfies the fixed-session CodingAgent contract directly (not through AgentRunner) -- start_session/2 once, run_turn/4 called twice with the exact same session value, both returning {:ok, turn_result}, verified via the existing fake-codex shell-script trace fixture pattern (SYMP_TEST_CODEx_TRACE) showing exactly 1 thread/start and 2 turn/start JSON-RPC messages -- the direct proof that one session/thread persists across multiple turns after T017/T018's behaviour extraction. Also asserts stop_session/1 is :ok and tolerates being called twice (documented idempotence in contracts/coding-agent-behaviour.md). Deliberately did NOT assert on turn_result.session_id: re-read the contract first and confirmed the generic CodingAgent callback only requires {:ok, turn_result :: map()} -- session_id is a Codex-specific incidental field, not a generic behavior requirement, so pinning it would test the wrong altitude. No function_exported?/3 tautology added since @behaviour already gives compile-time callback-shape checking. Zero production changes (no lib/ files touched); confirmed app_server_test.exs (19/19), dynamic_tool_test.exs (16/16), and live_e2e_test.exs (2 skipped, correctly gated behind SYMPHONY_RUN_LIVE_E2E=1, not enabled) all pass unmodified. Full gate clean from 6a5ac37's baseline: mix format --check-formatted, mix specs.check, mix credo --strict (0 issues), mix test 367/0/6 (366 baseline + 1 new test, 6 skipped matches baseline exactly), mix dialyzer 0 errors, mix test --cover 100% total incl. new file, mix build (escript) succeeds, git diff --check clean. tasks.md T019 checked off only; T020-T037 untouched; no claude_code/ lib directory exists yet. [elixir/test/symphony_elixir/coding_agent_test.exs:1]
- Sanitized development-only git history (21 commits, main..development) to strip Claude-Session/Co-Authored-By attribution trailers from 18 commit messages via plumbing rewrite (git commit-tree), preserving trees/authors/dates exactly; force-pushed with --force-with-lease. main untouched. GitHub still serves old contaminated SHAs by direct API/URL until GitHub's own GC runs (not immediately purged).
- Implemented T020 (001-local-tracker-multi-agent): lib/symphony_elixir/claude_code/mcp_server.ex, a new SymphonyElixir.ClaudeCode.MCPServer module hosting one per-run Bandit HTTP listener bound to 127.0.0.1:0. It is a bare Plug (init/1+call/2, no Phoenix/Endpoint) started directly via Bandit.start_link(plug: {__MODULE__, plug_state}, ...) with plug_state = %{tracker_binding:, issue:, token:} captured once at start -- no GenServer, no lookup table, no global/shared state, matching research.md R6/R6a's per-run-listener-holds-its-own-binding design. Implements the minimal MCP Streamable HTTP transport needed for claude's MCP client: POST /mcp/<token> carrying JSON-RPC 2.0 messages (initialize, notifications/initialized, tools/list, tools/call -> Tracker.execute_bound_agent_tool/4, method-not-found fallback); GET on the same path returns 405 (spec-compliant no-SSE-support signal) instead of a generic 404. The per-run token is a 32-byte crypto.strong_rand_bytes value, url_encode64-encoded (URL-path-safe, unlike http_server.ex's plain encode64 used only in a header), checked via Plug.Crypto.secure_compare/2 (already a transitive dep via Plug -- no new dependency added). start_link/1 returns {:ok, %{pid:, port:, token:}}, resolving the actual bound ephemeral port via ThousandIsland.listener_info/1 (Bandit.start_link/1's {:ok, pid} IS the ThousandIsland supervisor pid). stop/1 accepts either the pid or that map, checks Process.alive?/1 first, and catches :exit so it tolerates an already-stopped listener -- mirroring Codex.AppServer.stop_port/1's tolerance pattern, since T022 (out of scope this session) will call this from stop_session/1's after block on every exit path. Verified end-to-end via two non-committed scratch scripts (not part of the test suite): initialize/tools-list/notification/wrong-token-401 round-trip, and a second script wiring a real Local.Store + Local.Adapter + Local.AgentTool binding through two concurrent MCPServer instances, confirming a real tools/call for local_tracker_set_state lands on the local tracker JSON file and that run B's token against run A's port is rejected with 401 (cross-run isolation). Gates run for this task: mix format --check-formatted clean, mix specs.check clean, mix credo --strict 0 issues, mix dialyzer 0 errors, mix test 367/0/6 (unchanged from T019 baseline -- no test file added, since T021 is tasks.md's own explicit test task for this module, mirroring the T004/T006, T008/T011, T017/T019 implementation-then-test split precedent), mix build (escript) succeeds, git diff --check clean. T021-T037 untouched; tasks.md T020 checked off only. [elixir/lib/symphony_elixir/claude_code/mcp_server.ex:1] [elixir/lib/symphony_elixir/claude_code/mcp_server.ex:1]
- Implemented T021 (001-local-tracker-multi-agent): added test/symphony_elixir/claude_code_mcp_server_test.exs (24 tests, use ExUnit.Case async: true, no dependency on the shared WorkflowStore/Config singleton -- builds a Tracker.bind_agent_tools()-shaped binding map by hand around an in-test FakeAdapter module + an Agent-based recorder, decoupled from any real tracker adapter). Covers: loopback ephemeral-port startup; initialize (protocolVersion echo + default) and notifications/initialized; tools/list advertising the bound adapter's tool_specs; tools/call dispatching to Tracker.execute_bound_agent_tool/4 with the run's bound issue (proven via the recorder capturing issue.id, never anything from the request itself); unsupported-tool isError; JSON-RPC method-not-found and unrecognized-notification handling; malformed-JSON parse error (-32700); a message with no method (400 invalid_request); an oversized (>8MB) body forcing Plug.Conn.read_body's {:more,_,_} branch (400 invalid_request); wrong/missing token (401/404); GET rejected regardless of token validity (405, no SSE support); an unrelated path (404); stop/1 idempotence; and full two-instance cross-run isolation (distinct ports/tokens, run A's token against run B's port rejected and vice versa, each run's tool call landing only on its own recorder/issue). Production fix (justified, not scope creep): start_link/1's Bandit.start_link/1 call, on a real failure (:eaddrinuse reproduced via probe script), delivers a raw linked-process :EXIT signal that crashes any non-trapping caller (confirmed: the bare ExUnit test process crashed before the test assertion could run) instead of the {:error, term()} the function's own @spec already promised -- fixed by wrapping the Bandit.start_link/1 call in a new private start_bandit/3 that temporarily sets Process.flag(:trap_exit, true) (save/restore via try/after) so the documented {:ok,_}|{:error,_} contract actually holds for any caller, not just one that happens to trap exits. This matters beyond the test: T022's future ClaudeCode.AppServer.start_session/2 would otherwise crash instead of returning {:error, reason} on a real port-bind failure. No transport redesign, no new dependency -- minimal, localized to the one failure path the tests exposed. All gates verified clean: mix format --check-formatted, mix specs.check, mix credo --strict (0 issues), mix dialyzer (0 errors), mix test 389/0/6 (383 baseline-equivalent + new file's tests, 6 skipped matches baseline), mix test --cover 100% total (ClaudeCode.MCPServer 93.62% -> 100.00%, closing the T020 note's expected temporary drop), mix build (escript) succeeds, git diff --check clean. tasks.md T021 checked off only; T022-T037 untouched; no lib/ file touched other than the one-function fix in mcp_server.ex itself. [elixir/test/symphony_elixir/claude_code_mcp_server_test.exs:1]
- Implemented tasks.md T024-T026 (001-local-tracker-multi-agent): coding-agent execution selector config surface. T024: added agent_execution embed (kind: string, default codex) and sibling claude_code embed (command default 'claude --bare --permission-mode bypassPermissions --strict-mcp-config' per contracts/workflow-config-fields.md, turn_timeout_ms default 3_600_000, read_timeout_ms default 5_000) to lib/symphony_elixir/config/schema.ex, mirroring the existing Codex embed's changeset shape (validate_required(:command), whitespace-only-blank guard, validate_number on both timeouts). T025: added a cond clause to Config.validate_settings/1 (config.ex) rejecting agent_execution.kind: claude_code combined with non-empty worker.ssh_hosts, returning {:invalid_workflow_config, message} (same class/format every other config error already uses, verbatim message from research.md R6a/contracts doc); wired agent_execution_kind into WorkflowStore.compute_structural/2's structural snapshot map in both clauses (local-tracker and generic), so it is captured once at init/1 and pinned across reload_path/2 automatically (structural map is swapped wholesale, no per-field enumeration needed at the reload call site). T026: added test/symphony_elixir/config_claude_code_worker_host_validation_test.exs (6 tests: omitted-kind-defaults-to-codex, ssh_hosts-alone-with-codex-unaffected, claude_code+ssh_hosts-rejected-with-exact-message, valid-claude_code-config-accepted, command blank/whitespace rejection mirroring codex.command's own two-case test precedent from core_test.exs:64-74, timeout-must-be-positive for both turn_timeout_ms/read_timeout_ms) plus one restart-only structural regression test added to the existing test/symphony_elixir/config_structural_snapshot_test.exs (per explicit instruction, placed alongside the analogous tracker.kind restart-only test rather than in the new file). Extended test/support/test_support.exs's shared WORKFLOW.md fixture with agent_execution_kind/claude_code_command/claude_code_turn_timeout_ms/claude_code_read_timeout_ms overrides that render nothing when unset (mirrors the existing worker_yaml/server_yaml 'omit section when nil' precedent exactly) -- this was a deliberate design choice to preserve the literal absence of agent_execution/claude_code keys in the default fixture used by ~40 other test files (true backward-compat test, not just a defaulted-value test), while still letting the new file opt in via overrides. Verified: mix.exs's test_coverage.ignore_modules already exempts Config entirely, so validate_settings/1's new cond branch needed no coverage-driving test; Config.Schema and WorkflowStore are NOT exempted, so the new AgentExecution/ClaudeCode embeds and the two compute_structural/2 clauses do require full coverage -- confirmed 100% per-module in mix test --cover output (SymphonyElixir.Config.Schema.AgentExecution, SymphonyElixir.Config.Schema.ClaudeCode, SymphonyElixir.WorkflowStore all listed at 100.00%). All 6 gates verified clean: mix format --check-formatted, mix specs.check, mix credo --strict (0 issues), mix dialyzer (0 errors), mix test --cover (396 tests, 0 failures, 6 skipped, 100% total coverage every module -- re-run across 3 seeds, the known pre-existing flaky orchestrator_status_test.exs:971 wall-clock test (#0002) did not reproduce in any run), mix build (escript). Scope discipline: did not touch T022/T023 (ClaudeCode.AppServer), did not add validate_inclusion for agent_execution.kind/tracker.kind (no schema-level inclusion check exists for tracker.kind either -- that validation lives at the adapter-resolution point, deferred to T027 same as tracker's own precedent), did not add an unattended-approval-mode field to claude_code embed despite contracts/workflow-config-fields.md line 16 mentioning it as an example -- T024's own task text explicitly scopes the claude_code embed to exactly command/turn_timeout_ms/read_timeout_ms, so held to that literal list. [elixir/lib/symphony_elixir/config/schema.ex]
- Implemented T022-T023 (001-local-tracker-multi-agent): SymphonyElixir.ClaudeCode.AppServer (lib/symphony_elixir/claude_code/app_server.ex), the second CodingAgent behaviour implementation. start_session/2: rejects non-nil worker_host with {:error, :remote_worker_not_supported} (defense in depth); requires opts[:issue] (contract's 'opts at minimum worker_host' extended with :issue for Claude Code specifically, since R6a requires the MCP listener bound to one issue at start_session time, unlike Codex which doesn't need issue until run_turn); validates workspace cwd via a small deliberate duplication of Codex's local-only PathSafety branch (workspace safety is integration-agnostic per IV-003/IV-006; premature shared abstraction avoided per architecture guidance); starts one ClaudeCode.MCPServer (T020) and writes a per-run --mcp-config JSON to a tmp path. run_turn/4: spawns 'claude -p ...' via structured argv (never a shell string) with --session-id on turn 1 / --resume on later turns, tracked via an :atomics ref embedded in the session map (mutable per-session turn-order state without a global registry or extra process); explicit env allow-list (PATH/HOME/USER/SHELL/LANG/LC_ALL/LC_CTYPE/TERM/TMPDIR/ANTHROPIC_API_KEY) unsets every other currently-set env var by name, so OPENAI_API_KEY/Codex secrets never reach the child even though Codex's own port-env pattern is additive-inherit-then-unset-specific-names (an insufficient pattern for Claude's default-deny requirement); stderr is NOT merged into the parsed stdout stream (kept separate deliberately, confirmed correct by the real-CLI smoke run: a 'no stdin data' CLI diagnostic went to stderr, never polluting the stream-json parser); parser only special-cases system/init (-> :session_started) and result (-> terminal :turn_completed/:turn_failed via is_error), tolerating every other type/subtype (including newly-observed system/status, system/thinking_tokens, and top-level 'user' tool-result messages) via a generic passthrough -- confirmed sufficient by a real tool-invoking smoke run, no special-casing needed since MCP tool calls are fully out-of-band over HTTP, invisible to this stdout stream. close_port/1 now explicitly SIGKILLs the underlying OS pid (via :erlang.port_info/2's os_pid) before Port.close/1, not just Port.close alone -- Port.close does not reliably terminate a :spawn_executable child; discovered via a real leaked 'sleep' orphan process during test iteration on the timeout tests, fixed before completion (Codex.AppServer's own long-lived-port stop_port/1 does not do this, but Claude's per-turn subprocess lifecycle makes leaks concrete and immediate per timeout/crash test, unlike Codex's single per-run process). stop_session/1 stops the MCP listener and rm's the temp --mcp-config file, tolerating both already gone. Added SymphonyElixir.ClaudeCode.AppServer to mix.exs test_coverage.ignore_modules, mirroring the existing Codex.AppServer/Codex.DynamicTool precedent for subprocess-heavy modules with many defensive branches impractical to hit 100% via fixtures -- deliberate, precedent-following, not scope creep. Added test/symphony_elixir/claude_code_app_server_test.exs (15 tests) covering: remote-worker rejection before other validation; missing-issue failure without raising; real MCP wiring end-to-end (starts a real Local tracker + Local.Store, drives tools/list and tools/call over real HTTP against the started MCPServer via Req, confirms the mutation lands on the correct bound issue via Local.Adapter refetch); session-id-then-resume across 2 turns via a fake-claude shell-script fixture capturing its own argv; two independent sessions never cross-resume each other's session id; env-isolation (OPENAI_API_KEY/an arbitrary unrelated var excluded, ANTHROPIC_API_KEY included) via a fixture that dumps its own env to a file; missing-executable launch failure surfacing as {:error, {:claude_executable_not_found, _}} without crashing; tolerance of non-JSON/unknown-type stream lines; synthetic is_error:true and malformed-result (missing is_error) fixtures, explicitly documented in research.md as fixture-only since the real CLI smoke run never produced a real failure path (not faked); unexpected subprocess exit mid-turn -> {:error, {:port_exit, status}}; read_timeout_ms (bootstrap phase, before system/init) and turn_timeout_ms (after system/init, before result) timeout paths with tiny configured ms values (no slow tests); stop_session/1 cleanup (listener stopped, config file removed) and its double-call/already-stopped tolerance. Before completion, ran a real, non-committed, non-fixture smoke verification against the installed claude CLI v2.1.246: started a real ClaudeCode.MCPServer bound to a real established Local.Store/Local.Adapter tracker with one seeded issue, wrote a real --mcp-config, and ran a real 'claude -p' turn instructing it to call local_tracker_set_state -- confirmed via independent Local.Adapter.fetch_issues_by_ids/1 refetch that the issue's state genuinely changed from todo to done, i.e. the full MCP HTTP round trip (T020/T021's own module) worked for real, not simulated. A second real '--resume <same-uuid>' turn confirmed R7's previously-open continuation question: session_id echoes identically across the resume turn (system/init still emitted on resume, contrary to a plausible guess it might not) and conversational context is genuinely retained. New real event shapes observed and confirmed to need zero parser changes (already tolerated generically): system/status recurring multiple times per turn, system/thinking_tokens, and a top-level 'user' type carrying MCP tool_result content (including an internal tool_reference/deferred-tool-selection artifact preceding the real tool_use, mechanically similar to this session's own ToolSearch pattern but entirely internal to the CLI, never a distinct wire call to Symphony's MCP server). Also discovered and documented (research.md addendum, not fixed -- out of T022/T023 correctness scope): claude prints a 'no stdin data received in 3s' diagnostic to its own stderr (confirmed distinct from the parsed stdout stream) when launched with an open-but-unused stdin pipe (Port.open's default), adding a fixed ~3s startup latency to every real turn; flagged for T027+ to reconsider, not addressed now since closing/redirecting a Port's stdin independently has no portable non-shell-string mechanism and 3s is a rounding error against real turn runtimes. Failure-path result (is_error:true) remains genuinely unobserved from the real CLI (not attempted further, per explicit 'do not fake certainty' instruction -- inducing a real API/model failure safely and deterministically in a live smoke check is not practical). All 6 gates verified clean from 0405947's baseline: mix format --check-formatted, mix specs.check, mix credo --strict (0 issues), mix dialyzer (0 errors), mix test --cover (411 tests, 0 failures, 6 skipped -- 396 baseline + 15 new -- 100% coverage total, ClaudeCode.AppServer exempted per above), mix build (escript) succeeds. Did not implement T027 (AgentRunner dispatch wiring), did not add agent_execution.kind selector validation, did not begin T029+ -- ClaudeCode.AppServer exists and is fully tested directly but remains unreachable through normal Symphony dispatch until T027. [elixir/lib/symphony_elixir/claude_code/app_server.ex:1]
- Repaired T022/T023 ClaudeCode.AppServer per an independent adversarial review of commit 918f845, scope strictly limited to T022/T023 (no T027+ touched). Fixed BLOCKING first-turn/resume defect: run_turn/4's :atomics claim previously flipped unconditionally at call entry before confirming the CLI ever established a session (system/init), so a launch/bootstrap failure permanently left the session believing it was resumable, and any same-session retry would incorrectly launch --resume against a session the CLI never created (not reachable via current AgentRunner/Orchestrator call patterns -- retries always spawn a brand-new session -- but a real latent defect). Fixed by making the exchange-based claim provisional: revert_unestablished_claim/3 reverts turn_state back to 0 only when the claiming call itself never observed system/init (tracked via the receive loop's :bootstrap/:turn phase, now threaded through the return value), and only the call that made the original claim may ever revert it, so a later turn's own failure on an already-established session never re-arms --session-id. This required also fixing handle_incoming/5, which previously hardcoded the next phase to :turn on every branch except the literal system/init line, silently ending the bootstrap timeout window on any pre-init noise line. Added 4 regression tests proving retry-after-crash/timeout/executable-not-found all still use --session-id, and retry-after-established-turn-failure correctly still uses --resume (no over-revert). Addressed MAJOR coverage-exclusion gap: validate_workspace_cwd's duplicated branches and partial-start cleanup paths (write_mcp_config failure after listener start, MCPServer.start_link failure) had zero test coverage anywhere despite Codex.AppServer's identical logic being directly tested in its own suite -- added 3 workspace-boundary tests mirroring Codex's precedent exactly, plus 2 partial-start cleanup tests using two small purely-additive test-injection seams (opts[:mcp_start_opts], opts[:mcp_config_dir], both default to production behavior when omitted). Retained the module-wide mix.exs coverage exclusion (documented with an explicit rationale comment) since genuinely nondeterministic Port/OS-process branches remain, but this was a reassessed decision, not the original precedent accepted at face value. Documented (not changed) the MAJOR claude_code.command-vs-codex.command parsing divergence: codex.command is shell-interpolated (bash -lc, genuine quoting/operator support), claude_code.command is a naive whitespace-split argv list (no quoting, no shell, direct :spawn_executable -- intentionally safer against injection, retained per explicit repo-evidence-based decision, not changed to bash -lc for symmetry) -- documented in workflow-config-fields.md and resolve_command/0's own comment, pinned down by 2 new tests. Updated contracts/coding-agent-behaviour.md to explicitly document opts[:issue] as a Claude-Code-specific required start_session/2 extension key, harmless to Codex.AppServer (which ignores unrecognized opts keys), closing a documentation gap without changing the implementation. All 6 verification gates pass clean from this repair: mix format --check-formatted, mix specs.check, mix credo --strict (0 issues), mix dialyzer (0 errors), mix test --cover (422 tests, 0 failures, 6 skipped -- 411 baseline + 11 new -- 100% coverage on every non-excluded module), mix build (escript), git diff --check clean. T027's hardcoded codex.stall_timeout_ms constraint (Claude-Code-backed runs would share Codex's stall watchdog config once T027 wires dispatch) is documented as an explicit unresolved constraint for T027, not fixed here -- explicitly out of scope for this repair. [elixir/lib/symphony_elixir/claude_code/app_server.ex:1] [elixir/test/symphony_elixir/claude_code_app_server_test.exs:1]
- Implemented tasks.md T027-T028 (001-local-tracker-multi-agent): coding-agent dispatch resolution. T027: added SymphonyElixir.CodingAgent.resolve/0 + for_kind/1 (coding_agent.ex) resolving the concrete implementation module from the structural agent_execution.kind snapshot (Config.structural_settings!().agent_execution_kind, T025's pin) via a %{"codex" => Codex.AppServer, "claude_code" => ClaudeCode.AppServer} map -- mirroring Tracker.adapter/0 + adapter_for_kind/1's identical precedent exactly. agent_runner.ex's run_codex_turns/5 now calls CodingAgent.resolve() once per run and threads the resolved module (bundled into a run_state map alongside session/workspace/opts/issue_state_fetcher/max_turns to keep do_run_codex_turns's arity at 3, satisfying credo's max-arity-8 rule after adding the coding_agent field) through do_run_codex_turns instead of hardcoding Codex.AppServer's alias; start_session/2 now receives issue: issue unconditionally alongside worker_host, matching contracts/coding-agent-behaviour.md's documented harmless-for-Codex requirement. Also closed a gap T025's own commit note flagged as deferred to T027: agent_execution.kind previously had no inclusion validation (a bogus value would have passed startup config validation silently) -- Config.validate_settings/1 now calls CodingAgent.for_kind/1 and rejects an unsupported kind the same way Tracker.validate_config/1 already rejects an unsupported tracker.kind. Default/omitted agent_execution.kind still resolves to "codex" (Schema.AgentExecution's own Ecto default, unchanged), so existing Codex deployments are unaffected. T028: added test/symphony_elixir/agent_runner_dispatch_test.exs (5 tests) proving dispatch through AgentRunner.run/3's ordinary execution boundary (not just a module-mapping helper): default/no-selector -> Codex, explicit codex -> Codex, explicit claude_code -> ClaudeCode.AppServer (also proving issue: reached it, since ClaudeCode.AppServer.start_session/2 requires opts[:issue] or returns {:error, :issue_required}), OPENAI_API_KEY/unrelated env excluded from a claude_code-dispatched subprocess, and a live (non-restarted) agent_execution.kind edit does not redirect an in-flight/next dispatch (structural pin proof at the AgentRunner boundary, complementing config_structural_snapshot_test.exs's Config-level version). Each dispatch test also sets the *inactive* provider's command to a nonexistent/garbage binary path to prove it is never consulted (FR-009 isolation). Added 3 more tests to coding_agent_test.exs (CodingAgent.for_kind/1 mapping + rejection, resolve/0 structural-pin proof) and 1 to config_claude_code_worker_host_validation_test.exs (startup validation now rejects an unsupported agent_execution.kind) as direct regression coverage for the T027 production changes. stall_timeout_ms decision (explicitly required by the task prompt): confirmed via research.md's T022/T023-repair addendum that orchestrator.ex:582 reads codex.stall_timeout_ms unconditionally regardless of agent_execution.kind, with no claude_code.stall_timeout_ms field (T024 deliberately scoped that embed to command/turn_timeout_ms/read_timeout_ms only). Re-examined against frozen spec.md IV-002 ("Reconciliation, stall detection, and retry/backoff timing remain unchanged regardless of which coding-agent execution integration a run uses") and concluded the shared value is the CORRECT reading of that invariant, not a gap: a provider-specific stall timeout would make stall-detection timing vary by provider, which IV-002 explicitly forbids. Left unchanged; documented with an inline comment at orchestrator.ex:581 (citing IV-002) rather than inventing a new config field or broadening T027's scope, per the task's explicit instruction for this exact scenario. Verification: mix compile --warnings-as-errors, full mix test (431 tests/0 failures/6 skipped, run twice), mix test --cover (100% total, every non-exempted module 100.00% incl. CodingAgent; AgentRunner/Config/Orchestrator/Codex.AppServer/ClaudeCode.AppServer remain in mix.exs's pre-existing ignore_modules list, unchanged by this session), mix format --check-formatted, mix specs.check, mix credo --strict (0 issues -- fixed one arity-9 and one line-length finding via the run_state-map refactor above), mix dialyzer (0 errors), mix build (escript), git diff --check (clean). One new pre-existing-class flaky test discovered incidental to running the full suite twice: core_test.exs:1065 (same wall-clock-margin pattern as issue #0002, different test/location, confirmed unrelated to this work) -- logged as issue #0005, not fixed (out of scope). Scope discipline: did not touch T029 (live E2E), T030 (regression-verification), or any US3/Polish task; git diff --stat confirms only agent_runner.ex/coding_agent.ex/config.ex/orchestrator.ex (production) plus 3 test files touched, tasks.md T027/T028 flipped to [x].
- Implemented tasks.md T029-T030 (001-local-tracker-multi-agent): live E2E + regression verification checkpoint. T029: added test/symphony_elixir/claude_code_live_e2e_test.exs, flag-gated identically to live_e2e_test.exs's SYMPHONY_RUN_LIVE_E2E=1 convention (@moduletag :live_e2e, @tag skip: reason when the flag is absent -- verified the skip path directly: 1 test, 0 failures, 1 skipped). Design: exercises the real, ordinary AgentRunner.run/3 dispatch boundary (same pattern live_e2e_test.exs already uses for Codex -- not a bypass) against a tracker.kind: local + agent_execution.kind: claude_code deployment (not tracker.kind: memory, so the acceptance signal can be the local tracker JSON record's state changing on disk, independent of anything Claude reports in its own output) -- seeds one dispatch-eligible local-tracker issue via Local.Init.run/1 + a direct data-file write (config_structural_snapshot_test.exs's established precedent for seeding), pins tracker.kind/agent_execution.kind structurally via restart_workflow_store!(), then asserts session_started + turn_completed lifecycle events arrive via the same {:codex_worker_update, issue_id, message} channel Codex uses, and independently re-fetches the issue via Tracker.fetch_issues_by_ids/1 after the run to confirm state == "done" was actually written by the real MCP tool call (claude CLI -> ClaudeCode.MCPServer -> Tracker.execute_bound_agent_tool/4 -> Local.Store), not merely claimed. Verified the flag-present-but-prerequisite-missing path fails clearly (not silently, not by crashing): with SYMPHONY_RUN_LIVE_E2E=1 set, the test flunks with an exact, actionable message naming ANTHROPIC_API_KEY as the missing prerequisite, confirmed by direct probe of the real installed claude v2.1.246 CLI (--bare mode returns apiKeySource:"none"/"Not logged in" in this environment, which is authenticated only via OAuth/keychain for the ambient Claude Code session, not an API key -- contracts/workflow-config-fields.md already documents --bare as forcing API-key-only auth with no OAuth/keychain fallback, so this is expected/correct behavior, not a defect). Genuinely live-verified end-to-end run against the real claude CLI/API was NOT performed this session -- ANTHROPIC_API_KEY is unavailable in this environment (no env var, no keychain entry, no repo .env) -- so T029 is implemented and its gating/skip/prerequisite-failure behavior is directly verified, but the task's own actual live-success acceptance criterion (a real claude_code-backed item reaching a terminal succeeded state) remains unconfirmed and T029 is NOT marked complete in tasks.md, per explicit instruction not to claim completion without the prerequisite. T030: ran the exact regression surface tasks.md names (app_server_test.exs, dynamic_tool_test.exs, live_e2e_test.exs, orchestrator_status_test.exs, cli_test.exs, github/gitlab/jira/asana/local_adapter_test.exs, plus coding_agent_test.exs/agent_runner_dispatch_test.exs for T017/T018/T027's own dispatch-resolution regression coverage) -- 152 tests, 0 failures, 2 skipped (the flag-gated live_e2e_test.exs cases, correctly). Full mix test: 432 tests, 1 failure on the first run (core_test.exs:1025 'normal worker exit schedules active-state continuation retry', a wall-clock-margin assertion off by ~42ms under contention -- logged as new issue #0006, same pre-existing flaky class as #0002/#0005, a third distinct location, confirmed unrelated via a clean immediate rerun: 432/432 tests, 0 failures) -- core_test.exs is untouched by this session's changes. mix test --cover: 100% coverage every non-exempted module (ClaudeCode.MCPServer, CodingAgent, Config.Schema and all its embeds, every tracker adapter, WorkflowStore all 100.00%). All other gates clean: mix format --check-formatted, mix specs.check, mix credo --strict (0 issues), mix dialyzer (0 errors), mix build (escript succeeds), git diff --check (clean). T030 marked [x] in tasks.md (genuinely satisfied -- zero behavioral change confirmed for the existing Codex/hosted-tracker path). T029 left [ ] pending real ANTHROPIC_API_KEY availability to complete an actual live run. Files changed: elixir/test/symphony_elixir/claude_code_live_e2e_test.exs (new), specs/001-local-tracker-multi-agent/tasks.md (T030 checked off only). No production source touched -- T029/T030 are verification/test-only tasks per their own tasks.md scope, and the live run never actually reached production code paths this session since it never got past the prerequisite check. T031+ untouched; not committed. [specs/001-local-tracker-multi-agent/tasks.md]
- Resolved the T029-blocking open question from session #71 (can Claude Code subprocess auth use subscription/OAuth instead of ANTHROPIC_API_KEY while preserving isolation): YES, via --safe-mode instead of --bare. Primary evidence: claude --help's --bare description explicitly states 'Anthropic auth is strictly ANTHROPIC_API_KEY or apiKeyHelper via --settings (OAuth and keychain are never read)' -- --bare is the sole gate on OAuth, not a general isolation mechanism. Empirically verified against the real native claude binary (/Users/thomasestep/.local/bin/claude v2.1.246, NOT the dev sandbox's cmux-cli-shims wrapper on PATH, which proxies back into the live session and is not production-representative) with env restricted to the exact allow-list claude_subprocess_env/0 already uses (PATH/HOME/USER/SHELL/LANG/LC_ALL/LC_CTYPE/TERM/TMPDIR, no ANTHROPIC_API_KEY): --safe-mode + -p + --output-format stream-json + --verbose + --permission-mode bypassPermissions succeeded with apiKeySource:none and real subscription rate_limit_event telemetry (proving OAuth/keychain was actually used, not an API key), worked non-interactively/no-TTY, and --session-id then --resume across two turns correctly preserved conversational context (a planted secret word was recalled) -- matching Symphony's real per-turn relaunch pattern exactly. Also confirmed empirically: WITHOUT --safe-mode (and without --bare), an ambient plugin hook actually fired (a real 'SessionEnd hook' execution attempt visible on stderr) -- proving hooks are a genuine ambient-config leak risk when both flags are absent, and confirming --safe-mode suppresses it (no such hook fired under --safe-mode). Also confirmed --safe-mode blocks ambient user-level CLAUDE.md content from reaching the model's context (a targeted recall probe answered NO). MCP isolation is independently owned by --strict-mcp-config (already in the default command) + explicit --mcp-config, not by --bare/--safe-mode at all -- so this swap does not touch MCP isolation. One residual open item, not fully closed: plugins/skills/slash-commands/agents still appear enumerated in the system/init event's metadata under --safe-mode (unlike --bare, which suppresses auto-discovery entirely) -- confirmed CLAUDE.md-injection and hook-execution are blocked, but did not exhaustively prove every enumerated item is fully inert. Recommendation (not yet implemented): keep --bare as the schema default (safest, requires zero trust in ambient host config) but document --safe-mode as the supported claude_code.command override an operator can set in their own WORKFLOW.md today, with zero production code change, to get subscription-backed auth -- and use exactly that override to unblock T029's live run by updating only the test's own WORKFLOW.md fixture + its require_anthropic_api_key! gate (both in the new, uncommitted claude_code_live_e2e_test.exs), not the production default. Did not implement this or any other change; purely an investigation this session, no application source touched, not committed. [elixir/lib/symphony_elixir/claude_code/app_server.ex:22]
- Refined the T029-blocking auth investigation further: --safe-mode (last session's recommendation) is NOT the right answer for the intended local execution model because it is scope-blind -- it disables ALL customizations (CLAUDE.md, hooks, plugins, skills, MCP) regardless of whether they're repo-owned or user-global, per its own full doc text ("Start with all customizations disabled... CLAUDE.md, skills, plugins, hooks, MCP servers... do not load"). The actual scope-aware mechanism is --setting-sources <user,project,local>, which gates which settings.json files load (~/.claude/settings.json=user, .claude/settings.json=project/shared/committed, .claude/settings.local.json=local/personal-per-repo) -- and per code.claude.com/docs/en/mcp and /permissions, this flag ALSO independently gates project .mcp.json loading and (confirmed empirically) plugin enumeration, while built-in bundled skills remain always available (not a leak, not user-installed). Primary evidence: code.claude.com/docs/en/permissions' "What runs before you trust a folder" table states that in claude -p (headless, Symphony's exact mode) with the folder never trusted, project hooks are "Used" unconditionally and project .mcp.json servers are "Connected without asking, approved or not" -- i.e., repo-owned hooks/MCP already work headlessly today with ZERO extra flags, no approval gate; the piece needing explicit suppression is user-global config. Empirically verified end-to-end in one combined run against the real native claude binary (not the dev sandbox's cmux shim), restricted to Symphony's exact env allow-list, no ANTHROPIC_API_KEY, using: normal auth mode (no --bare, no --safe-mode) + --setting-sources project,local (excludes "user") + --strict-mcp-config --mcp-config with two explicit files (the repo's own .mcp.json plus Symphony's generated per-run config -- both explicit, repo MCP composes with Symphony's per-run MCP without weakening --strict-mcp-config at all): (1) a scratch repo's own CLAUDE.md loaded (model echoed a planted phrase) while the real ambient ~/.claude/CLAUDE.md did NOT leak (targeted recall probe answered NO); (2) a scratch repo-scoped SessionStart hook fired (marker file created) while the real ambient plugin hook (ponytail's SessionEnd, previously observed firing under plain normal-mode with no --setting-sources) did NOT fire (empty stderr, no "SessionEnd hook" message); (3) system/init's mcp_servers listed both the repo-scoped probe server and Symphony's explicit server as attempted, while the real ambient user-scope MCP server "context7" (confirmed present in ~/.claude.json's real mcpServers block) never appeared; (4) plugins enumeration dropped from the real 6 installed marketplace plugins (seen under both plain normal-mode and --safe-mode in the prior session) to an empty list; (5) apiKeySource stayed "none" and a real --session-id then --resume pair correctly preserved context (planted word recalled), proving OAuth/subscription auth is fully compatible with this configuration, non-interactively, no TTY. This is a strictly better fit than --safe-mode for Symphony's stated local-developer trust model (repo-owned context allowed, user-global excluded, subscription auth allowed) because it is scope-aware rather than an all-or-nothing kill switch. Recommendation (not yet implemented): claude_code.command should use --setting-sources project,local --permission-mode bypassPermissions --strict-mcp-config plus --mcp-config naming the repo's own .mcp.json (if present) alongside Symphony's generated per-run config, instead of --bare, when an operator wants subscription-backed auth with repo-scoped Claude context; keep --bare as the schema default for operators who want the strictest, config-blind isolation. One residual, undecided risk worth flagging in docs: per the same "what runs before you trust a folder" table, this configuration means any hook or .mcp.json server committed to a repository's .claude/settings.json/.mcp.json executes/connects completely unconditionally the first time Symphony dispatches a claude_code turn against that workspace -- no approval step, no workspace-trust dialog (headless mode never shows one). This is consistent with Symphony's own trust boundary (it already executes arbitrary repo code via the coding agent) but is a materially different risk profile from --bare's total silence on repo content, and should be called out explicitly if this configuration becomes documented guidance. Did not implement any change to schema.ex, contracts, or the live e2e test this session -- investigation only, no application source touched, not committed. [elixir/lib/symphony_elixir/claude_code/app_server.ex:22]
- Implemented tasks.md T029 (001-local-tracker-multi-agent): reconciled the pre-existing paused test/symphony_elixir/claude_code_live_e2e_test.exs (untracked artifact from before T028A's repair) with the repaired T028A local-execution contract. Reused ~95% of the file as-is (AgentRunner.run/3 dispatch design, local-tracker-on-disk independent verification, lifecycle event draining, workflow/data seeding) -- it was already well-designed, just stale on auth. The only change: replaced require_anthropic_api_key!/0 (which flunked unless ANTHROPIC_API_KEY was set, referencing the now-obsolete --bare default) with require_claude_authenticated!/0, which runs 'claude auth status --json' (no model call, no cost) and flunks with an actionable message referencing the new --setting-sources project,local default and 'claude auth login' if loggedIn is not true. The test does not override claude_code.command, so it exercises the schema's actual default (T028A's repaired --setting-sources project,local --permission-mode bypassPermissions --strict-mcp-config) end to end -- this IS the proof that the repaired default authenticates via subscription/OAuth without ANTHROPIC_API_KEY, not a separate assertion. Verified both non-live paths directly: default (no SYMPHONY_RUN_LIVE_E2E) -> skips cleanly (1 test, 0 failures, 1 skipped); SYMPHONY_RUN_LIVE_E2E=1 with prerequisites present -> in this development sandbox, the test did NOT stop at prerequisite-checking, it ran the full live path to completion and PASSED (session_started + turn_completed lifecycle events observed, refute turn_failed held, and Tracker.fetch_issues_by_ids/1 independently confirmed the local-tracker JSON record's state changed from todo to done via a real MCP tool round trip: claude CLI -> ClaudeCode.MCPServer -> Tracker.execute_bound_agent_tool/4 -> Local.Store). IMPORTANT CAVEAT, must not be dropped in future sessions: 'claude' on PATH inside this coding-agent sandbox resolves to a cmux-cli-shims wrapper (confirmed via 'which claude'), the same shim a prior session (evt_efbce62302e7443c891d) explicitly characterized as proxying back into the live session and 'not production-representative', distinct from the real native binary at ~/.local/bin/claude that prior investigation sessions deliberately used for their empirical auth probes. This session's actual live pass therefore proves the test's mechanics and the AgentRunner->ClaudeCode.AppServer->real subprocess->MCP->Local.Store integration boundary genuinely work, and that 'claude auth status --json' is a correct, cost-free prerequisite gate -- but does NOT independently re-confirm (beyond what prior investigation sessions already established) that a fully separate, non-proxied native claude installation authenticates identically outside this sandbox; that was already established empirically in prior sessions (evt_7cf07a191739410a8be7) against the real native binary, not re-verified here. A real live model turn DID execute this session (unavoidable per T029's own stated acceptance criteria once prerequisites are met and the flag is set) -- not repeated after the first successful run. Full verification suite clean: mix format --check-formatted, mix compile --warnings-as-errors, mix specs.check, mix credo --strict (0 issues), mix dialyzer (0 errors), mix test (438 tests, 0 failures, 7 skipped), mix test --cover (100% every module), git diff --check clean. tasks.md T029 flipped to [x]. Files changed: elixir/test/symphony_elixir/claude_code_live_e2e_test.exs (reconciled, now tracked), specs/001-local-tracker-multi-agent/tasks.md (T029 checked off). No production source touched. [elixir/test/symphony_elixir/claude_code_live_e2e_test.exs]
- Implemented tasks.md T031 (001-local-tracker-multi-agent): base US3 composition scenario. Added test/symphony_elixir/local_tracker_claude_code_composition_test.exs -- a deterministic (non-flag-gated) counterpart to claude_code_live_e2e_test.exs (T029): seeds one dispatch-eligible tracker.kind: local issue via Local.Init.run/1 + a direct data-file write, configures agent_execution.kind: claude_code with claude_code.command pointing at a fake-claude shell fixture (mirrors claude_code_app_server_test.exs's established fake-executable pattern -- prints a system/init event then a result/success event, no MCP tool call), pins both structural selectors via restart_workflow_store!() (IV-005), then dispatches through the real AgentRunner.run/3 boundary (max_turns: 1) and asserts session_started + turn_completed lifecycle events arrive via the same {:codex_worker_update, ...} channel Codex uses, with no turn_failed. Scope decision: T031's own text (spec AS1 -- 'Symphony dispatches, executes, and completes the run using only those two components') is about the run-attempt lifecycle completing, not the tracker item's business state reaching a terminal value -- that specific proof (the MCP local_tracker_set_state tool call landing on the exact bound issue record) is T032's explicitly separate, not-yet-built scope ('an MCP ... tool call -- issued by the fake-claude fixture's scripted turn' -- worded as a capability T032 adds, not one T031 already has). So T031's fixture deliberately does not call the MCP tool; AgentRunner.run/3 still returns :ok because do_run_codex_turns/3's max_turns-reached branch treats a normally-completed turn with the issue still active as a completed run attempt (agent_runner.ex:140-146), exactly mirroring how a Codex run that exhausts its turns without the tracker reaching a terminal state is still a completed attempt. No production code touched -- verification/test-only task per its own tasks.md scope. tasks.md T031 flipped to [x]. Verification: mix format --check-formatted, mix compile --warnings-as-errors, mix specs.check, mix credo --strict (0 issues), full mix test (439 tests, 0 failures, 7 skipped -- no flaky #0002/#0005/#0008 recurrence this run), mix test --cover (100% total, every non-exempted module 100.00%), mix dialyzer (0 errors), mix build (escript), git diff --check (clean). T032-T034 (US3 continuation) and T035-T037 (Polish, depend on all of Phase 5 per tasks.md's own phase-dependency note) deliberately left untouched this session. [elixir/test/symphony_elixir/local_tracker_claude_code_composition_test.exs:1]
- Implemented tasks.md T032 (001-local-tracker-multi-agent): added a second test to local_tracker_claude_code_composition_test.exs where the fake-claude fixture itself reads its own --mcp-config argv, extracts the real ephemeral symphony_tracker MCP URL, and curls a real JSON-RPC tools/call for local_tracker_set_state -- proving the Claude-side MCP tool-call path reaches ClaudeCode.MCPServer and mutates the exact bound issue record, verified by reading .symphony/local_tracker.json directly off disk (not lifecycle events or process stdout). No production code changed -- MCPServer/AgentTool/Store already fully supported this; only the test fixture was extended. [elixir/test/symphony_elixir/local_tracker_claude_code_composition_test.exs]
- Implemented tasks.md T033 (001-local-tracker-multi-agent): added a concurrency case to local_tracker_claude_code_composition_test.exs proving cross-run MCP isolation between two concurrent claude_code-backed tracker.kind:local AgentRunner.run/3 dispatches. Used a leak-file + go-marker barrier handshake (each fake-claude fixture leaks its real generated MCP URL, keyed by $PWD basename = issue identifier since claude_subprocess_env strips custom env vars, then blocks on a shared go marker) to force real overlap instead of sleep-and-hope, then ran a composition-scope mirror of claude_code_mcp_server_test.exs's T021 cross-token check (run A's token against run B's live port, and vice versa, both 401) while both listeners were live, then verified via direct on-disk read that each issue received only its own run's mutation. No production code changes were needed -- the existing per-run MCPServer/Local.Store architecture (T020/T022) already isolates by construction; this is a test-only proof at full composition scope.
- T034 (SC-004 equivalence): dispatch order, concurrency-limit enforcement, and retry/backoff decisions are structurally invariant to tracker.kind/agent_execution.kind -- Orchestrator.sort_issues_for_dispatch/should_dispatch_issue?/state_slots_available?/handle_agent_down all operate purely on Issue/State structs with zero kind-based branching. T034 proves this via a paired test driving the real (non-stubbed) Orchestrator functions under two live-reloaded deployment configs (tracker.kind: local + claude_code vs tracker.kind: memory + codex) with identical normalized Issue inputs, rather than adding new production code. [elixir/test/symphony_elixir/local_tracker_claude_code_composition_test.exs:412]
- Implemented tasks.md T035 (001-local-tracker-multi-agent): amended elixir/AGENTS.md's Docs Update Policy WORKFLOW.md bullet to explicitly name the local tracker (tracker.kind: local, symphony local-tracker init) and Claude Code execution (agent_execution.kind, claude_code.*) as new config-contract surface requiring doc updates; docs-only, no SPEC.md/README.md adapter sections added since T035's task text scoped only to elixir/AGENTS.md [elixir/AGENTS.md:70]
- T036: full-tree `make all` gate (setup, build, fmt-check, mix specs.check + credo --strict, coverage, dialyzer) run from elixir/ against HEAD d91c854 (T001-T035 committed) passed clean on first attempt: 442 tests, 0 failures, 7 skipped (flag-gated live-E2E requiring SYMPHONY_RUN_LIVE_E2E=1, expected/documented), 100% coverage across all non-excluded modules including every new Local.*/ClaudeCode.*/CodingAgent module, credo --strict 0 issues, dialyzer 0 errors, format check clean. None of the known flaky wall-clock timing tests (#0002/#0005/#0009 class) fired this run. No code changes were necessary; T036 marked [x] in tasks.md as bookkeeping-only commit. [specs/001-local-tracker-multi-agent/tasks.md:118]

## Notes
- Retry failed workspace setup and anchor local roots
- Block generic tool input and document idle timeouts
- Bump Symphony version to 0.0.2
- Add Symphony release skill
- Scrub GitHub and GitLab authentication token aliases (#119)
- mix test --cover after T020 alone drops the summary threshold from 100.00% to 95.22% -- SymphonyElixir.ClaudeCode.MCPServer (new this task) reports 0.00%, since nothing calls it yet (ClaudeCode.AppServer, which will wire it in via start_session/2, is T022, not yet built) and T020's own scope explicitly excludes writing its test file (that is T021's task per tasks.md, mirroring the T004/T006, T008/T011, T017/T019 implementation-then-test precedent). This is an expected, temporary consequence of the T020/T021 split, not a regression or a weakened gate -- mix format/specs.check/credo --strict/dialyzer/mix test(no --cover)/mix build all pass clean at T020's own commit boundary. T021 (test/symphony_elixir/claude_code_mcp_server_test.exs) is expected to bring coverage back to 100% by exercising initialize/tools-list/tools-call/token-rejection/cross-run-isolation against the real module (already hand-verified via non-committed scratch scripts during T020, so T021's assertions have a known-working target). [elixir/lib/symphony_elixir/claude_code/mcp_server.ex:1]
- T029 (001-local-tracker-multi-agent) paused, not abandoned: live Claude Code E2E verification is blocked on a potentially production-relevant open question, not a test-fixture gap. The question (to investigate in a FUTURE session, not resolved now): can Symphony's Claude Code integration support unattended subprocess execution backed by an existing Claude Pro/Max subscription OAuth login, while preserving the credential/MCP/ambient-config isolation guarantees claude_code.command's current --bare default provides? No conclusion has been reached. This session deliberately did NOT: remove or weaken --bare as a test-only workaround, request/fabricate/persist an ANTHROPIC_API_KEY, or perform further OAuth/subscription-auth experiments -- per explicit user instruction, since changing the auth model is a production design decision requiring its own deliberation, not something to shortcut just to make one test pass. The next session should investigate supported production authentication semantics (does the installed claude CLI's OAuth/keychain path work at all under a non-interactive/headless subprocess launch? what isolation properties would be lost or need reinvention if --bare were dropped or made conditional?) before resuming T029's actual live run. T029's test file (test/symphony_elixir/claude_code_live_e2e_test.exs) is fully implemented and its skip/prerequisite-failure paths are directly verified (skips cleanly without SYMPHONY_RUN_LIVE_E2E=1; with the flag set, flunks with an exact ANTHROPIC_API_KEY-missing message) -- only the actual successful live run against the real API remains unperformed. tasks.md T029 left unchecked; T030 checked (independently complete -- full Codex/hosted-tracker regression suite, unrelated to this blocker). [specs/001-local-tracker-multi-agent/tasks.md]
- Correction: the preceding 'fix' event on issue #0006 was misattributed. Issue #0006 is the pre-existing, unrelated flaky test test/symphony_elixir/core_test.exs:1025 (wall-clock margin under scheduler contention); the fix text attached to it actually describes tasks.md T028A's claude_code auth/MCP-composition repair (config/schema.ex, claude_code/app_server.ex), which has nothing to do with core_test.exs. This happened because 'pjm fix' was invoked without --issue and defaulted to closing the stale .current_issue pointer (#0006) instead of leaving it untouched. Re-logging #0006's original flaky-test defect as a new open issue below since events.jsonl is append-only and #0006 cannot be reopened in place; #0006 itself should be read as closed-in-error, not as evidence the flaky test was fixed. [test/symphony_elixir/core_test.exs:1025]
- Correction: the preceding 'fix' event on issue #0007 was misattributed. Issue #0007 is the pre-existing, unrelated flaky test test/symphony_elixir/core_test.exs:1025 (wall-clock margin under scheduler contention, re-logged copy of #0006's original defect); the fix text attached to it actually describes this session's T028A review-fix pass (MCP config composition-order collision fix in ClaudeCode.AppServer), which has nothing to do with core_test.exs. This happened because 'pjm fix' was invoked without --issue and defaulted to closing the stale .current_issue pointer (#0007) instead of leaving it untouched -- the exact same mistake previously documented on #0006's own correction note. Re-logging #0007's original flaky-test defect as a new open issue (#0008) since events.jsonl is append-only and #0007 cannot be reopened in place; #0007 itself should be read as closed-in-error, not as evidence the flaky test was fixed. [test/symphony_elixir/core_test.exs:1025]
- Correction: the preceding 'fix' event on issue #0008 was misattributed. Issue #0008 is the pre-existing, unrelated flaky test test/symphony_elixir/core_test.exs:1025 (wall-clock margin under scheduler contention, re-logged copy of #0006/#0007's original defect); the fix text attached to it actually describes this session's T034 SC-004 equivalence test work, which has nothing to do with core_test.exs. This happened because 'pjm fix' was invoked without --issue and defaulted to closing the stale .current_issue pointer (#0008) instead of leaving it untouched -- the exact same mistake previously documented on #0006's and #0007's own correction notes (now a 3rd recurrence). Re-logging #0008's original flaky-test defect as a new open issue below since events.jsonl is append-only and #0008 cannot be reopened in place; #0008 itself should be read as closed-in-error, not as evidence the flaky test was fixed. [test/symphony_elixir/core_test.exs:1025]

## Key files
- `elixir/lib/symphony_elixir/config.ex`
- `elixir/lib/symphony_elixir/workflow_store.ex`
- `elixir/test/symphony_elixir/core_test.exs`
- `elixir/test/symphony_elixir/extensions_test.exs`
- `elixir/lib/symphony_elixir.ex`
- `elixir/lib/symphony_elixir/agent_runtime_supervisor.ex`
- `elixir/lib/symphony_elixir/orchestrator.ex`
- `elixir/test/symphony_elixir/live_e2e_test.exs`
- `elixir/test/symphony_elixir/orchestrator_status_test.exs`
- `elixir/config/config.exs`
- `elixir/lib/symphony_elixir/config/schema.ex`
- `elixir/test/fixtures/startup_workflow.md`
- `elixir/test/symphony_elixir/workspace_and_config_test.exs`
- `elixir/AGENTS.md`
- `.github/workflows/burrito-release.yml`
- `elixir/.gitignore`
- `elixir/README.md`
- `elixir/lib/symphony_elixir/cli.ex`
- `elixir/mix.exs`
- `elixir/mix.lock`

## Open questions
- None logged yet.
